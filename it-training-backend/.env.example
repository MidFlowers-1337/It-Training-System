# IT Training System - Environment Variables Template
# ============================================================
# 复制此文件为 .env.local 并填入真实值（.env.local 不会被提交到 Git）
# 
# 安全警告：
# - 生产环境必须配置所有标记为 [必需] 的变量
# - 切勿将真实凭据提交到版本控制系统
# ============================================================

# ========== 数据库配置 [必需] ==========
# 数据库连接 URL
# 开发环境示例：jdbc:mysql://localhost:3306/it_training?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
# 生产环境示例：jdbc:mysql://your-db-host:3306/it_training?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai
DB_URL=jdbc:mysql://localhost:3306/it_training?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true

# 数据库用户名
DB_USERNAME=root

# 数据库密码（生产环境必须使用强密码）
DB_PASSWORD=your_secure_password

# TiDB Cloud 远程数据库配置（可选，用于远程开发）
# DEV_REMOTE_DB_URL=jdbc:mysql://your-tidb-host:4000/it_training?sslMode=VERIFY_IDENTITY&enabledTLSProtocols=TLSv1.2,TLSv1.3
# DEV_REMOTE_DB_USERNAME=your_username
# DEV_REMOTE_DB_PASSWORD=your_password

# ========== JWT 安全配置 [必需] ==========
# JWT 密钥（生产环境必须修改！）
# 要求：至少 256 位（32 字符以上），使用随机生成的强密钥
# 生成方法：openssl rand -base64 64 | tr -d '\n'
JWT_SECRET=YourProductionSecretKeyMustBeAtLeast256BitsLongAndSecure123456789

# JWT 过期时间（毫秒）
# 默认 24 小时 = 86400000
# 生产环境建议：1-4 小时 = 3600000-14400000
JWT_EXPIRATION=86400000

# ========== CORS 跨域配置 [生产环境必需] ==========
# 允许的来源列表（逗号分隔）
# 开发环境默认：http://localhost:5173,http://localhost:3000
# 生产环境示例：https://your-domain.com,https://www.your-domain.com
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ========== AI 服务配置 ==========
# DeepSeek API 密钥
AI_API_KEY=sk-your-deepseek-api-key

# AI 服务地址（可选，默认 DeepSeek）
AI_BASE_URL=https://api.deepseek.com

# AI 模型（可选）
AI_MODEL=deepseek-chat

# ========== Spring Profile 配置 ==========
# 激活的配置文件：dev（开发）、prod（生产）、test（测试）
# SPRING_PROFILES_ACTIVE=dev

# ============================================================
# 使用说明
# ============================================================
# 
# 1. 将此文件复制为 .env.local：
#    cp .env.example .env.local
#
# 2. 编辑 .env.local，替换所有占位符为真实值
#
# 3. 启动方式：
#
#    方式一：IDEA/VSCode 运行配置
#    - 在运行配置中添加环境变量文件
#
#    方式二：命令行手动设置
#
#    Windows CMD:
#      set DB_URL=jdbc:mysql://localhost:3306/it_training?...
#      set DB_USERNAME=root
#      set DB_PASSWORD=your_password
#      set JWT_SECRET=your_jwt_secret
#      mvn spring-boot:run
#
#    PowerShell:
#      $env:DB_URL="jdbc:mysql://localhost:3306/it_training?..."
#      $env:DB_USERNAME="root"
#      $env:DB_PASSWORD="your_password"
#      $env:JWT_SECRET="your_jwt_secret"
#      mvn spring-boot:run
#
#    Linux/Mac:
#      export DB_URL=jdbc:mysql://localhost:3306/it_training?...
#      export DB_USERNAME=root
#      export DB_PASSWORD=your_password
#      export JWT_SECRET=your_jwt_secret
#      mvn spring-boot:run
#
#    方式三：使用 dotenv-java 或 spring-dotenv 自动加载
#
# ============================================================
# 安全检查清单（部署前必须确认）
# ============================================================
# [ ] DB_PASSWORD 已设置为强密码
# [ ] JWT_SECRET 已设置为随机生成的 256 位密钥
# [ ] CORS_ALLOWED_ORIGINS 已限制为生产域名
# [ ] 所有敏感配置未提交到 Git
# ============================================================