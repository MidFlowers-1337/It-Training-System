# IT 技能培训智能选课系统 - 页面模块拆分文档

> 文档版本：v1.0.0
> 最后更新：2025-12-25
> 文档状态：🚧 进行中
> 关联文档：[01-重构总览.md](01-重构总览.md) | [03-API对接.md](03-API对接.md) | [PagesAndFlows.md](../PagesAndFlows.md)

---

## 目录

1. [页面总览](#1-页面总览)
2. [公共页面](#2-公共页面)
3. [管理端页面](#3-管理端页面)
4. [讲师端页面](#4-讲师端页面)
5. [学员端页面](#5-学员端页面)
6. [路由配置](#6-路由配置)

---

## 1. 页面总览

### 1.1 页面结构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           页面结构总览                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  公共页面 (无需登录)                                                          │
│  ├── /login              登录页                                              │
│  └── /register           注册页                                              │
│                                                                             │
│  管理端 (ADMIN)                                                              │
│  ├── /admin/dashboard         统计看板                                       │
│  ├── /admin/users            用户管理                                        │
│  ├── /admin/courses          课程管理                                        │
│  ├── /admin/sessions         班期管理                                        │
│  └── /admin/enrollments      报名管理                                        │
│                                                                             │
│  讲师端 (INSTRUCTOR)                                                         │
│  ├── /instructor/sessions    我的班期                                        │
│  └── /instructor/students    学员名单                                        │
│                                                                             │
│  学员端 (STUDENT)                                                            │
│  ├── /student/home           首页                                            │
│  ├── /student/courses        课程列表                                        │
│  ├── /student/courses/:id    课程详情                                        │
│  ├── /student/enrollments    我的报名                                        │
│  ├── /student/ai-recommend   AI 智能推荐                                     │
│  ├── /student/dashboard      学习仪表盘                                      │
│  ├── /student/plans          学习计划                                        │
│  ├── /student/achievements   成就系统                                        │
│  └── /student/profile        个人中心                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 页面与主题对应

| 页面类型 | Dark 主题特点 | Light 主题特点 | Enterprise 主题特点 |
|---------|--------------|---------------|-------------------|
| 登录/注册 | 渐变背景+毛玻璃表单 | 黑白分割+线条装饰 | 渐变光晕+彩色阴影卡片 |
| 管理端 | 深色侧边栏+数据卡片 | 极简线条布局 | 蓝紫渐变标题+浅色卡片 |
| 学员端 | 动态光斑+鼠标追踪 | 编辑风格排版 | 3D卡片+悬浮效果 |

---

## 2. 公共页面

### 2.1 登录页 (Login.vue)

#### 功能描述
- 用户名/密码登录
- 记住我功能
- 忘记密码链接
- 跳转注册页
- 登录成功后根据角色跳转

#### 组件拆分

```
views/public/Login.vue
├── components/
│   ├── LoginForm.vue          # 登录表单
│   ├── ThemeBg.vue            # 主题背景容器
│   └── AuthLogo.vue           # Logo 组件
```

#### 页面结构

```vue
<!-- views/public/Login.vue -->
<script setup lang="ts">
import { ref } from 'vue'
import { useAuthStore } from '@/stores/auth'
import { useThemeStore } from '@/stores/theme'
import LoginForm from './components/LoginForm.vue'

// Dark 主题专属组件
import DarkGradientBackground from '@/components/theme/dark/GradientBackground.vue'
// Light 主题专属组件
import LightTextureOverlay from '@/components/theme/light/TextureOverlay.vue'
// Enterprise 主题专属组件
import EnterpriseGradientOrbs from '@/components/theme/enterprise/GradientOrbs.vue'

const authStore = useAuthStore()
const themeStore = useThemeStore()
</script>

<template>
  <div class="login-page">
    <!-- Dark 主题背景 -->
    <DarkGradientBackground v-if="themeStore.currentTheme === 'dark'" />

    <!-- Light 主题背景 -->
    <div v-else-if="themeStore.currentTheme === 'light'" class="light-bg">
      <LightTextureOverlay type="grid" />
    </div>

    <!-- Enterprise 主题背景 -->
    <EnterpriseGradientOrbs v-else />

    <!-- 登录表单容器 -->
    <div class="login-container">
      <div class="login-card">
        <h1 class="login-title">欢迎登录</h1>
        <p class="login-subtitle">IT 技能培训智能选课系统</p>
        <LoginForm @success="handleLoginSuccess" />
        <div class="login-footer">
          <span>还没有账号？</span>
          <router-link to="/register">立即注册</router-link>
        </div>
      </div>
    </div>
  </div>
</template>
```

#### 登录表单组件

```vue
<!-- views/public/components/LoginForm.vue -->
<script setup lang="ts">
import { ref, reactive } from 'vue'
import { useAuthStore } from '@/stores/auth'
import { useNotification } from '@/composables/useNotification'
import BaseInput from '@/components/base/Input/Input.vue'
import BaseButton from '@/components/base/Button/Button.vue'

const emit = defineEmits<{
  (e: 'success'): void
}>()

const authStore = useAuthStore()
const { error: showError } = useNotification()

const form = reactive({
  username: '',
  password: '',
  rememberMe: false
})

const loading = ref(false)
const errors = reactive({
  username: '',
  password: ''
})

// 表单验证
const validate = () => {
  let valid = true
  errors.username = ''
  errors.password = ''

  if (!form.username.trim()) {
    errors.username = '请输入用户名'
    valid = false
  }

  if (!form.password) {
    errors.password = '请输入密码'
    valid = false
  } else if (form.password.length < 6) {
    errors.password = '密码至少 6 位'
    valid = false
  }

  return valid
}

// 提交登录
const handleSubmit = async () => {
  if (!validate()) return

  loading.value = true
  try {
    await authStore.login({
      username: form.username,
      password: form.password
    })
    emit('success')
  } catch (err: any) {
    showError('登录失败', err.message || '用户名或密码错误')
  } finally {
    loading.value = false
  }
}
</script>

<template>
  <form class="login-form" @submit.prevent="handleSubmit">
    <div class="form-group">
      <BaseInput
        v-model="form.username"
        label="用户名"
        placeholder="请输入用户名"
        :error="errors.username"
        autocomplete="username"
      />
    </div>

    <div class="form-group">
      <BaseInput
        v-model="form.password"
        type="password"
        label="密码"
        placeholder="请输入密码"
        :error="errors.password"
        autocomplete="current-password"
      />
    </div>

    <div class="form-options">
      <label class="remember-me">
        <input type="checkbox" v-model="form.rememberMe" />
        <span>记住我</span>
      </label>
      <router-link to="/forgot-password" class="forgot-link">
        忘记密码？
      </router-link>
    </div>

    <BaseButton
      type="submit"
      variant="primary"
      :loading="loading"
      block
    >
      登录
    </BaseButton>
  </form>
</template>
```

#### 主题样式差异

```css
/* Dark 主题样式 */
[data-theme="dark"] .login-page {
  background: var(--background-base);
}

[data-theme="dark"] .login-card {
  background: var(--surface);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-card);
  backdrop-filter: blur(20px);
}

[data-theme="dark"] .login-title {
  background: linear-gradient(to bottom, #fff, rgba(255,255,255,0.7));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* Light 主题样式 */
[data-theme="light"] .login-page {
  background: var(--background);
  display: grid;
  grid-template-columns: 1fr 1fr;
}

[data-theme="light"] .login-card {
  background: var(--background);
  border: 2px solid var(--foreground);
  border-radius: 0;  /* 无圆角 */
  box-shadow: none;  /* 无阴影 */
}

[data-theme="light"] .login-title {
  font-family: var(--font-display);  /* Playfair Display */
  font-size: 3rem;
  color: var(--foreground);
}

/* Enterprise 主题样式 */
[data-theme="enterprise"] .login-page {
  background: var(--background);
}

[data-theme="enterprise"] .login-card {
  background: var(--foreground);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
}

[data-theme="enterprise"] .login-title {
  background: var(--gradient-primary);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
```

### 2.2 注册页 (Register.vue)

#### 功能描述
- 用户名、密码、确认密码
- 真实姓名、邮箱、手机号
- 表单验证
- 注册成功跳转登录

#### 组件拆分

```
views/public/Register.vue
├── components/
│   ├── RegisterForm.vue       # 注册表单
│   └── PasswordStrength.vue   # 密码强度指示器
```

#### 核心实现

```vue
<!-- views/public/components/RegisterForm.vue -->
<script setup lang="ts">
import { ref, reactive, computed } from 'vue'
import { useRouter } from 'vue-router'
import { authApi } from '@/api/auth'
import { useNotification } from '@/composables/useNotification'
import BaseInput from '@/components/base/Input/Input.vue'
import BaseButton from '@/components/base/Button/Button.vue'
import PasswordStrength from './PasswordStrength.vue'

const router = useRouter()
const { success, error: showError } = useNotification()

const form = reactive({
  username: '',
  password: '',
  confirmPassword: '',
  realName: '',
  email: '',
  phone: ''
})

const loading = ref(false)
const errors = reactive<Record<string, string>>({})

// 密码强度计算
const passwordStrength = computed(() => {
  const pwd = form.password
  if (!pwd) return 0
  let score = 0
  if (pwd.length >= 8) score++
  if (/[a-z]/.test(pwd) && /[A-Z]/.test(pwd)) score++
  if (/\d/.test(pwd)) score++
  if (/[!@#$%^&*]/.test(pwd)) score++
  return score
})

// 表单验证
const validate = () => {
  const errs: Record<string, string> = {}

  if (!form.username.trim()) {
    errs.username = '请输入用户名'
  } else if (form.username.length < 3) {
    errs.username = '用户名至少 3 个字符'
  }

  if (!form.password) {
    errs.password = '请输入密码'
  } else if (form.password.length < 6) {
    errs.password = '密码至少 6 位'
  }

  if (form.password !== form.confirmPassword) {
    errs.confirmPassword = '两次密码不一致'
  }

  if (!form.realName.trim()) {
    errs.realName = '请输入真实姓名'
  }

  if (!form.email.trim()) {
    errs.email = '请输入邮箱'
  } else if (!/^[\w-]+(\.[\w-]+)*@[\w-]+(\.[\w-]+)+$/.test(form.email)) {
    errs.email = '邮箱格式不正确'
  }

  Object.assign(errors, errs)
  return Object.keys(errs).length === 0
}

// 提交注册
const handleSubmit = async () => {
  if (!validate()) return

  loading.value = true
  try {
    await authApi.register({
      username: form.username,
      password: form.password,
      realName: form.realName,
      email: form.email,
      phone: form.phone || undefined
    })
    success('注册成功', '请使用新账号登录')
    router.push('/login')
  } catch (err: any) {
    showError('注册失败', err.message || '请稍后重试')
  } finally {
    loading.value = false
  }
}
</script>

<template>
  <form class="register-form" @submit.prevent="handleSubmit">
    <div class="form-row">
      <BaseInput
        v-model="form.username"
        label="用户名"
        placeholder="请输入用户名"
        :error="errors.username"
        required
      />
    </div>

    <div class="form-row">
      <BaseInput
        v-model="form.realName"
        label="真实姓名"
        placeholder="请输入真实姓名"
        :error="errors.realName"
        required
      />
    </div>

    <div class="form-row">
      <BaseInput
        v-model="form.email"
        type="email"
        label="邮箱"
        placeholder="请输入邮箱"
        :error="errors.email"
        required
      />
    </div>

    <div class="form-row">
      <BaseInput
        v-model="form.phone"
        label="手机号"
        placeholder="请输入手机号（选填）"
        :error="errors.phone"
      />
    </div>

    <div class="form-row">
      <BaseInput
        v-model="form.password"
        type="password"
        label="密码"
        placeholder="请输入密码"
        :error="errors.password"
        required
      />
      <PasswordStrength :strength="passwordStrength" />
    </div>

    <div class="form-row">
      <BaseInput
        v-model="form.confirmPassword"
        type="password"
        label="确认密码"
        placeholder="请再次输入密码"
        :error="errors.confirmPassword"
        required
      />
    </div>

    <BaseButton
      type="submit"
      variant="primary"
      :loading="loading"
      block
    >
      注册
    </BaseButton>

    <p class="login-link">
      已有账号？<router-link to="/login">立即登录</router-link>
    </p>
  </form>
</template>
```

---

## 3. 管理端页面

### 3.1 管理端布局 (AdminLayout.vue)

#### 功能描述
- 左侧固定侧边栏导航
- 顶部标题栏（面包屑+用户信息+主题切换）
- 主内容区域
- 响应式折叠

#### 组件拆分

```
components/layout/AdminLayout/
├── AdminLayout.vue            # 布局主组件
├── AdminSidebar.vue           # 侧边栏
├── AdminHeader.vue            # 顶部栏
├── AdminMenuItem.vue          # 菜单项
└── AdminBreadcrumb.vue        # 面包屑
```

#### 布局实现

```vue
<!-- components/layout/AdminLayout/AdminLayout.vue -->
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useRoute } from 'vue-router'
import AdminSidebar from './AdminSidebar.vue'
import AdminHeader from './AdminHeader.vue'

const route = useRoute()
const collapsed = ref(false)

const toggleSidebar = () => {
  collapsed.value = !collapsed.value
}

const sidebarWidth = computed(() => collapsed.value ? '64px' : '240px')
</script>

<template>
  <div class="admin-layout" :class="{ collapsed }">
    <!-- 侧边栏 -->
    <AdminSidebar
      :collapsed="collapsed"
      @toggle="toggleSidebar"
    />

    <!-- 主区域 -->
    <div class="admin-main" :style="{ marginLeft: sidebarWidth }">
      <!-- 顶部栏 -->
      <AdminHeader />

      <!-- 内容区域 -->
      <main class="admin-content">
        <router-view />
      </main>
    </div>
  </div>
</template>

<style scoped>
.admin-layout {
  min-height: 100vh;
  background: var(--background-base, var(--background));
}

.admin-main {
  transition: margin-left var(--duration-normal) var(--ease-out);
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

.admin-content {
  flex: 1;
  padding: var(--spacing-lg);
  overflow-y: auto;
}
</style>
```

#### 侧边栏组件

```vue
<!-- components/layout/AdminLayout/AdminSidebar.vue -->
<script setup lang="ts">
import { computed } from 'vue'
import { useRoute } from 'vue-router'
import {
  LayoutDashboard,
  Users,
  BookOpen,
  Calendar,
  ClipboardList,
  ChevronLeft,
  ChevronRight
} from 'lucide-vue-next'
import AdminMenuItem from './AdminMenuItem.vue'

const props = defineProps<{
  collapsed: boolean
}>()

const emit = defineEmits<{
  (e: 'toggle'): void
}>()

const route = useRoute()

const menuItems = [
  { path: '/admin/dashboard', icon: LayoutDashboard, label: '统计看板' },
  { path: '/admin/users', icon: Users, label: '用户管理' },
  { path: '/admin/courses', icon: BookOpen, label: '课程管理' },
  { path: '/admin/sessions', icon: Calendar, label: '班期管理' },
  { path: '/admin/enrollments', icon: ClipboardList, label: '报名管理' }
]

const isActive = (path: string) => route.path === path
</script>

<template>
  <aside class="admin-sidebar" :class="{ collapsed }">
    <!-- Logo -->
    <div class="sidebar-logo">
      <img src="@/assets/logo.svg" alt="Logo" class="logo-img" />
      <span v-if="!collapsed" class="logo-text">IT 培训系统</span>
    </div>

    <!-- 导航菜单 -->
    <nav class="sidebar-nav">
      <AdminMenuItem
        v-for="item in menuItems"
        :key="item.path"
        :to="item.path"
        :icon="item.icon"
        :label="item.label"
        :collapsed="collapsed"
        :active="isActive(item.path)"
      />
    </nav>

    <!-- 折叠按钮 -->
    <button class="collapse-btn" @click="emit('toggle')">
      <ChevronLeft v-if="!collapsed" :size="18" />
      <ChevronRight v-else :size="18" />
    </button>
  </aside>
</template>
```

### 3.2 统计看板 (Dashboard.vue)

#### 功能描述
- 系统概览统计卡片（用户数、课程数、班期数、报名数）
- 报名趋势图表
- 课程分类统计
- 讲师排名
- 最近报名列表

#### 组件拆分

```
views/admin/Dashboard.vue
├── components/
│   ├── StatCard.vue           # 统计卡片
│   ├── EnrollmentTrendChart.vue  # 报名趋势图
│   ├── CategoryPieChart.vue   # 分类饼图
│   ├── InstructorRanking.vue  # 讲师排名
│   └── RecentEnrollments.vue  # 最近报名
```

#### 页面实现

```vue
<!-- views/admin/Dashboard.vue -->
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { statisticsApi } from '@/api/statistics'
import { enrollmentApi } from '@/api/enrollment'
import type { SystemOverview, EnrollmentTrend } from '@/types/statistics'
import type { Enrollment } from '@/types/enrollment'

import StatCard from './components/StatCard.vue'
import EnrollmentTrendChart from './components/EnrollmentTrendChart.vue'
import CategoryPieChart from './components/CategoryPieChart.vue'
import InstructorRanking from './components/InstructorRanking.vue'
import RecentEnrollments from './components/RecentEnrollments.vue'

// 数据状态
const overview = ref<SystemOverview | null>(null)
const enrollmentTrend = ref<EnrollmentTrend[]>([])
const categoryStats = ref<any[]>([])
const instructorRanking = ref<any[]>([])
const recentEnrollments = ref<Enrollment[]>([])

const loading = ref(true)

// 获取数据
onMounted(async () => {
  try {
    const [
      overviewRes,
      trendRes,
      categoryRes,
      rankingRes,
      enrollmentsRes
    ] = await Promise.all([
      statisticsApi.getOverview(),
      statisticsApi.getEnrollmentTrend({ period: 'DAILY' }),
      statisticsApi.getCategoryStatistics(),
      statisticsApi.getInstructorRanking({ limit: 5 }),
      enrollmentApi.getEnrollments({ page: 0, size: 10 })
    ])

    overview.value = overviewRes.data
    enrollmentTrend.value = trendRes.data
    categoryStats.value = categoryRes.data
    instructorRanking.value = rankingRes.data
    recentEnrollments.value = enrollmentsRes.data.content
  } finally {
    loading.value = false
  }
})
</script>

<template>
  <div class="admin-dashboard">
    <h1 class="page-title">统计看板</h1>

    <!-- 概览统计卡片 -->
    <section class="stats-section">
      <div class="stats-grid">
        <StatCard
          title="用户总数"
          :value="overview?.totalUsers ?? 0"
          icon="Users"
          trend="+12%"
          trend-type="up"
        />
        <StatCard
          title="课程总数"
          :value="overview?.totalCourses ?? 0"
          icon="BookOpen"
          trend="+5%"
          trend-type="up"
        />
        <StatCard
          title="进行中班期"
          :value="overview?.ongoingSessions ?? 0"
          icon="Calendar"
        />
        <StatCard
          title="报名总数"
          :value="overview?.totalEnrollments ?? 0"
          icon="ClipboardList"
          trend="+23%"
          trend-type="up"
        />
      </div>
    </section>

    <!-- 图表区域 -->
    <section class="charts-section">
      <div class="charts-grid">
        <!-- 报名趋势图 -->
        <div class="chart-card chart-large">
          <h3 class="chart-title">报名趋势</h3>
          <EnrollmentTrendChart :data="enrollmentTrend" />
        </div>

        <!-- 分类统计 -->
        <div class="chart-card">
          <h3 class="chart-title">课程分类分布</h3>
          <CategoryPieChart :data="categoryStats" />
        </div>
      </div>
    </section>

    <!-- 列表区域 -->
    <section class="lists-section">
      <div class="lists-grid">
        <!-- 讲师排名 -->
        <div class="list-card">
          <h3 class="list-title">讲师排名</h3>
          <InstructorRanking :data="instructorRanking" />
        </div>

        <!-- 最近报名 -->
        <div class="list-card list-large">
          <h3 class="list-title">最近报名</h3>
          <RecentEnrollments :data="recentEnrollments" />
        </div>
      </div>
    </section>
  </div>
</template>
```

### 3.3 用户管理 (UserManagement.vue)

#### 功能描述
- 用户列表（分页、搜索、筛选）
- 创建用户
- 编辑用户
- 删除用户
- 批量操作
- 重置密码

#### 组件拆分

```
views/admin/UserManagement.vue
├── components/
│   ├── UserTable.vue          # 用户表格
│   ├── UserForm.vue           # 用户表单（创建/编辑）
│   ├── UserFilter.vue         # 筛选条件
│   └── UserActions.vue        # 操作按钮组
```

#### 页面实现

```vue
<!-- views/admin/UserManagement.vue -->
<script setup lang="ts">
import { ref, reactive, onMounted, watch } from 'vue'
import { userApi, type UserQueryParams } from '@/api/user'
import type { User, UserForm as UserFormType, UserRole, UserStatus } from '@/types/user'
import type { PageResult } from '@/types/api'
import { useNotification } from '@/composables/useNotification'

import BaseButton from '@/components/base/Button/Button.vue'
import BaseModal from '@/components/base/Modal/Modal.vue'
import UserTable from './components/UserTable.vue'
import UserForm from './components/UserForm.vue'
import UserFilter from './components/UserFilter.vue'

const { success, error: showError } = useNotification()

// 列表数据
const users = ref<User[]>([])
const pagination = reactive({
  page: 0,
  size: 10,
  total: 0,
  totalPages: 0
})

// 筛选条件
const filters = reactive<UserQueryParams>({
  keyword: '',
  role: undefined,
  status: undefined
})

// 加载状态
const loading = ref(false)

// 弹窗状态
const showFormModal = ref(false)
const editingUser = ref<User | null>(null)
const formLoading = ref(false)

// 获取用户列表
const fetchUsers = async () => {
  loading.value = true
  try {
    const res = await userApi.getUsers({
      ...filters,
      page: pagination.page,
      size: pagination.size
    })
    users.value = res.data.content
    pagination.total = res.data.totalElements
    pagination.totalPages = res.data.totalPages
  } catch (err: any) {
    showError('获取用户列表失败', err.message)
  } finally {
    loading.value = false
  }
}

// 打开创建弹窗
const handleCreate = () => {
  editingUser.value = null
  showFormModal.value = true
}

// 打开编辑弹窗
const handleEdit = (user: User) => {
  editingUser.value = user
  showFormModal.value = true
}

// 提交表单
const handleFormSubmit = async (data: UserFormType) => {
  formLoading.value = true
  try {
    if (editingUser.value) {
      await userApi.updateUser(editingUser.value.id, data)
      success('更新成功')
    } else {
      await userApi.createUser(data)
      success('创建成功')
    }
    showFormModal.value = false
    fetchUsers()
  } catch (err: any) {
    showError('操作失败', err.message)
  } finally {
    formLoading.value = false
  }
}

// 删除用户
const handleDelete = async (user: User) => {
  if (!confirm(`确定要删除用户 "${user.realName}" 吗？`)) return

  try {
    await userApi.deleteUser(user.id)
    success('删除成功')
    fetchUsers()
  } catch (err: any) {
    showError('删除失败', err.message)
  }
}

// 重置密码
const handleResetPassword = async (user: User) => {
  if (!confirm(`确定要重置用户 "${user.realName}" 的密码吗？`)) return

  try {
    const res = await userApi.resetUserPassword(user.id)
    success('密码重置成功', `临时密码：${res.data.tempPassword}`)
  } catch (err: any) {
    showError('重置密码失败', err.message)
  }
}

// 分页变化
const handlePageChange = (page: number) => {
  pagination.page = page
  fetchUsers()
}

// 筛选变化
watch(filters, () => {
  pagination.page = 0
  fetchUsers()
}, { deep: true })

onMounted(fetchUsers)
</script>

<template>
  <div class="user-management">
    <div class="page-header">
      <h1 class="page-title">用户管理</h1>
      <BaseButton variant="primary" @click="handleCreate">
        新建用户
      </BaseButton>
    </div>

    <!-- 筛选栏 -->
    <UserFilter v-model="filters" />

    <!-- 用户表格 -->
    <UserTable
      :data="users"
      :loading="loading"
      :pagination="pagination"
      @edit="handleEdit"
      @delete="handleDelete"
      @reset-password="handleResetPassword"
      @page-change="handlePageChange"
    />

    <!-- 用户表单弹窗 -->
    <BaseModal
      v-model="showFormModal"
      :title="editingUser ? '编辑用户' : '新建用户'"
      width="600px"
    >
      <UserForm
        :user="editingUser"
        :loading="formLoading"
        @submit="handleFormSubmit"
        @cancel="showFormModal = false"
      />
    </BaseModal>
  </div>
</template>
```

### 3.4 课程管理 (CourseManagement.vue)

#### 功能描述
- 课程列表（卡片/表格视图）
- 创建课程
- 编辑课程
- 发布/下架课程
- 删除课程
- 查看课程详情和班期

#### 组件拆分

```
views/admin/CourseManagement.vue
├── components/
│   ├── CourseTable.vue        # 课程表格
│   ├── CourseCard.vue         # 课程卡片
│   ├── CourseForm.vue         # 课程表单
│   ├── CourseFilter.vue       # 筛选条件
│   └── CourseDetail.vue       # 课程详情
```

### 3.5 班期管理 (SessionManagement.vue)

#### 功能描述
- 班期列表
- 创建班期
- 编辑班期
- 查看学员名单
- 更新班期状态

#### 组件拆分

```
views/admin/SessionManagement.vue
├── components/
│   ├── SessionTable.vue       # 班期表格
│   ├── SessionForm.vue        # 班期表单
│   ├── SessionFilter.vue      # 筛选条件
│   ├── SessionStudents.vue    # 学员名单
│   └── SessionStatusTag.vue   # 状态标签
```

### 3.6 报名管理 (EnrollmentManagement.vue)

#### 功能描述
- 报名列表
- 审核报名（通过/拒绝）
- 批量审核
- 导出报名数据

#### 组件拆分

```
views/admin/EnrollmentManagement.vue
├── components/
│   ├── EnrollmentTable.vue    # 报名表格
│   ├── EnrollmentFilter.vue   # 筛选条件
│   ├── EnrollmentDetail.vue   # 报名详情
│   └── ApprovalModal.vue      # 审核弹窗
```

---

## 4. 讲师端页面

### 4.1 讲师端布局 (InstructorLayout.vue)

与管理端布局类似，但菜单项不同。

### 4.2 我的班期 (MySessions.vue)

#### 功能描述
- 查看自己负责的班期
- 筛选班期状态
- 查看班期详情
- 查看学员名单

### 4.3 学员名单 (StudentList.vue)

#### 功能描述
- 查看班期学员
- 学员学习进度
- 导出学员名单

---

## 5. 学员端页面

### 5.1 学员端布局 (StudentLayout.vue)

#### 功能描述
- 顶部导航栏
- 移动端汉堡菜单
- 主题切换
- 用户信息下拉

#### 组件拆分

```
components/layout/StudentLayout/
├── StudentLayout.vue          # 布局主组件
├── StudentNavbar.vue          # 顶部导航
├── StudentMobileMenu.vue      # 移动端菜单
├── UserDropdown.vue           # 用户下拉
└── ThemeSwitch.vue            # 主题切换
```

### 5.2 首页 (Home.vue)

#### 功能描述
- Hero 区域
- 热门课程推荐
- 即将开课班期
- 学习进度概览（已登录）
- 功能特性介绍

#### 组件拆分

```
views/student/Home.vue
├── components/
│   ├── HeroSection.vue        # Hero 区域
│   ├── FeaturedCourses.vue    # 热门课程
│   ├── UpcomingSessions.vue   # 即将开课
│   ├── LearningOverview.vue   # 学习概览
│   └── FeaturesSection.vue    # 功能特性
```

### 5.3 课程列表 (CourseList.vue)

#### 功能描述
- 课程搜索
- 分类筛选
- 难度筛选
- 课程卡片展示
- 分页

#### 组件拆分

```
views/student/CourseList.vue
├── components/
│   ├── CourseSearchBar.vue    # 搜索栏
│   ├── CourseFilters.vue      # 筛选面板
│   ├── CourseGrid.vue         # 课程网格
│   └── CourseListItem.vue     # 课程列表项
```

### 5.4 课程详情 (CourseDetail.vue)

#### 功能描述
- 课程信息展示
- 可选班期列表
- 报名操作
- 相似课程推荐

#### 组件拆分

```
views/student/CourseDetail.vue
├── components/
│   ├── CourseHeader.vue       # 课程头部
│   ├── CourseInfo.vue         # 课程信息
│   ├── SessionList.vue        # 班期列表
│   ├── EnrollButton.vue       # 报名按钮
│   └── SimilarCourses.vue     # 相似课程
```

### 5.5 AI 智能推荐 (AiRecommend.vue)

#### 功能描述
- 个性化课程推荐
- 推荐理由展示
- 学习路径推荐
- 技能差距分析
- 反馈推荐结果

#### 组件拆分

```
views/student/AiRecommend.vue
├── components/
│   ├── RecommendHeader.vue    # 推荐页头部
│   ├── RecommendCard.vue      # 推荐卡片
│   ├── LearningPathCard.vue   # 学习路径卡片
│   ├── SkillGapAnalysis.vue   # 技能差距分析
│   └── FeedbackButtons.vue    # 反馈按钮
```

### 5.6 学习仪表盘 (Dashboard.vue)

#### 功能描述
- 学习概览统计
- 本周学习时长图表
- 进行中的课程
- 待办事项
- 最近获得的成就
- 打卡入口

#### 组件拆分

```
views/student/Dashboard.vue
├── components/
│   ├── LearningStats.vue      # 学习统计
│   ├── WeeklyChart.vue        # 周学习图表
│   ├── OngoingCourses.vue     # 进行中课程
│   ├── TodoList.vue           # 待办事项
│   ├── RecentAchievements.vue # 最近成就
│   └── CheckinWidget.vue      # 打卡组件
```

### 5.7 学习计划 (LearningPlan.vue)

#### 功能描述
- 当前计划展示
- 创建新计划
- 编辑计划
- 目标进度跟踪
- 历史计划

#### 组件拆分

```
views/student/LearningPlan.vue
├── components/
│   ├── ActivePlan.vue         # 当前计划
│   ├── PlanForm.vue           # 计划表单
│   ├── GoalProgress.vue       # 目标进度
│   ├── PlanHistory.vue        # 历史计划
│   └── GoalItem.vue           # 目标项
```

### 5.8 成就系统 (Achievements.vue)

#### 功能描述
- 已解锁成就展示
- 未解锁成就进度
- 成就分类浏览
- 成就详情

#### 组件拆分

```
views/student/Achievements.vue
├── components/
│   ├── AchievementGrid.vue    # 成就网格
│   ├── AchievementCard.vue    # 成就卡片
│   ├── CategoryTabs.vue       # 分类标签
│   ├── ProgressBadge.vue      # 进度徽章
│   └── AchievementDetail.vue  # 成就详情
```

### 5.9 个人中心 (Profile.vue)

#### 功能描述
- 个人信息展示
- 编辑个人信息
- 修改密码
- 学习偏好设置
- 技能展示

#### 组件拆分

```
views/student/Profile.vue
├── components/
│   ├── ProfileHeader.vue      # 个人信息头部
│   ├── ProfileForm.vue        # 信息表单
│   ├── PasswordForm.vue       # 密码表单
│   ├── PreferencesForm.vue    # 偏好设置
│   ├── SkillTags.vue          # 技能标签
│   └── ActivityTimeline.vue   # 活动时间线
```

---

## 6. 路由配置

### 6.1 路由结构

```typescript
// router/index.ts
import { createRouter, createWebHistory } from 'vue-router'
import { useAuthStore } from '@/stores/auth'

// 布局组件
const AdminLayout = () => import('@/components/layout/AdminLayout/AdminLayout.vue')
const InstructorLayout = () => import('@/components/layout/InstructorLayout/InstructorLayout.vue')
const StudentLayout = () => import('@/components/layout/StudentLayout/StudentLayout.vue')

const router = createRouter({
  history: createWebHistory(),
  routes: [
    // 公共路由
    {
      path: '/login',
      name: 'Login',
      component: () => import('@/views/public/Login.vue'),
      meta: { guest: true }
    },
    {
      path: '/register',
      name: 'Register',
      component: () => import('@/views/public/Register.vue'),
      meta: { guest: true }
    },

    // 管理端路由
    {
      path: '/admin',
      component: AdminLayout,
      meta: { requiresAuth: true, roles: ['ADMIN'] },
      children: [
        {
          path: '',
          redirect: '/admin/dashboard'
        },
        {
          path: 'dashboard',
          name: 'AdminDashboard',
          component: () => import('@/views/admin/Dashboard.vue')
        },
        {
          path: 'users',
          name: 'UserManagement',
          component: () => import('@/views/admin/UserManagement.vue')
        },
        {
          path: 'courses',
          name: 'CourseManagement',
          component: () => import('@/views/admin/CourseManagement.vue')
        },
        {
          path: 'sessions',
          name: 'SessionManagement',
          component: () => import('@/views/admin/SessionManagement.vue')
        },
        {
          path: 'enrollments',
          name: 'EnrollmentManagement',
          component: () => import('@/views/admin/EnrollmentManagement.vue')
        }
      ]
    },

    // 讲师端路由
    {
      path: '/instructor',
      component: InstructorLayout,
      meta: { requiresAuth: true, roles: ['INSTRUCTOR'] },
      children: [
        {
          path: '',
          redirect: '/instructor/sessions'
        },
        {
          path: 'sessions',
          name: 'InstructorSessions',
          component: () => import('@/views/instructor/MySessions.vue')
        },
        {
          path: 'sessions/:id/students',
          name: 'SessionStudents',
          component: () => import('@/views/instructor/StudentList.vue')
        }
      ]
    },

    // 学员端路由
    {
      path: '/student',
      component: StudentLayout,
      meta: { requiresAuth: true, roles: ['STUDENT'] },
      children: [
        {
          path: '',
          redirect: '/student/home'
        },
        {
          path: 'home',
          name: 'StudentHome',
          component: () => import('@/views/student/Home.vue')
        },
        {
          path: 'courses',
          name: 'CourseList',
          component: () => import('@/views/student/CourseList.vue')
        },
        {
          path: 'courses/:id',
          name: 'CourseDetail',
          component: () => import('@/views/student/CourseDetail.vue')
        },
        {
          path: 'enrollments',
          name: 'MyEnrollments',
          component: () => import('@/views/student/MyEnrollments.vue')
        },
        {
          path: 'ai-recommend',
          name: 'AiRecommend',
          component: () => import('@/views/student/AiRecommend.vue')
        },
        {
          path: 'dashboard',
          name: 'LearningDashboard',
          component: () => import('@/views/student/Dashboard.vue')
        },
        {
          path: 'plans',
          name: 'LearningPlan',
          component: () => import('@/views/student/LearningPlan.vue')
        },
        {
          path: 'achievements',
          name: 'Achievements',
          component: () => import('@/views/student/Achievements.vue')
        },
        {
          path: 'profile',
          name: 'Profile',
          component: () => import('@/views/student/Profile.vue')
        }
      ]
    },

    // 根路由重定向
    {
      path: '/',
      redirect: '/login'
    },

    // 404
    {
      path: '/:pathMatch(.*)*',
      name: 'NotFound',
      component: () => import('@/views/public/NotFound.vue')
    }
  ]
})

export default router
```

### 6.2 路由守卫

```typescript
// router/guards.ts
import type { Router } from 'vue-router'
import { useAuthStore } from '@/stores/auth'

export function setupRouterGuards(router: Router) {
  router.beforeEach(async (to, from, next) => {
    const authStore = useAuthStore()

    // 检查是否需要认证
    const requiresAuth = to.matched.some(record => record.meta.requiresAuth)
    const isGuestRoute = to.matched.some(record => record.meta.guest)

    // 如果需要认证但未登录
    if (requiresAuth && !authStore.isAuthenticated) {
      return next({
        path: '/login',
        query: { redirect: to.fullPath }
      })
    }

    // 如果已登录但访问登录/注册页
    if (isGuestRoute && authStore.isAuthenticated) {
      // 根据角色跳转
      const role = authStore.userRole
      if (role === 'ADMIN') {
        return next('/admin/dashboard')
      } else if (role === 'INSTRUCTOR') {
        return next('/instructor/sessions')
      } else {
        return next('/student/home')
      }
    }

    // 检查角色权限
    const requiredRoles = to.meta.roles as string[] | undefined
    if (requiredRoles && authStore.user) {
      if (!requiredRoles.includes(authStore.user.role)) {
        // 无权限，重定向到对应端
        const role = authStore.userRole
        if (role === 'ADMIN') {
          return next('/admin/dashboard')
        } else if (role === 'INSTRUCTOR') {
          return next('/instructor/sessions')
        } else {
          return next('/student/home')
        }
      }
    }

    next()
  })
}
```

---

## 修订记录

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| v1.0.0 | 2025-12-25 | Frontend Architect | 初始版本 |
