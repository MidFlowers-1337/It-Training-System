# IT 技能培训智能选课系统 - 组件库设计文档

> 文档版本：v1.0.0
> 最后更新：2025-12-25
> 文档状态：🚧 进行中
> 关联文档：[01-重构总览.md](01-重构总览.md) | [02-主题系统设计.md](02-主题系统设计.md)

---

## 目录

1. [组件设计原则](#1-组件设计原则)
2. [基础组件](#2-基础组件)
3. [表单组件](#3-表单组件)
4. [反馈组件](#4-反馈组件)
5. [业务组件](#5-业务组件)
6. [布局组件](#6-布局组件)

---

## 1. 组件设计原则

### 1.1 设计理念

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           组件设计原则                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐    │
│  │   主题无关   │   │   单一职责   │   │   组合优先   │   │   类型安全   │    │
│  │  CSS 变量   │   │  功能聚焦   │   │  灵活组合    │   │ TypeScript  │    │
│  └─────────────┘   └─────────────┘   └─────────────┘   └─────────────┘    │
│                                                                             │
│  • 所有样式通过 CSS 变量实现，确保主题切换无需修改组件                         │
│  • 每个组件只负责一个功能，避免过度耦合                                       │
│  • 优先使用组合而非继承，通过 slots 提供灵活扩展                              │
│  • 所有组件使用 TypeScript，提供完整的类型定义                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 命名规范

| 类型 | 前缀 | 示例 |
|------|------|------|
| 基础组件 | Base | `BaseButton`, `BaseInput` |
| 表单组件 | Form | `FormField`, `FormSelect` |
| 业务组件 | 无（直接业务名） | `CourseCard`, `StatCard` |
| 布局组件 | Layout | `LayoutSidebar`, `LayoutNavbar` |
| 主题组件 | Theme + 主题名 | `ThemeDarkBlobs`, `ThemeLightLine` |

### 1.3 组件目录结构

```
components/
├── base/                    # 基础组件
│   ├── Button/
│   │   ├── Button.vue       # 组件实现
│   │   ├── Button.types.ts  # 类型定义
│   │   └── index.ts         # 导出
│   ├── Input/
│   ├── Card/
│   ├── Table/
│   ├── Modal/
│   └── ...
│
├── form/                    # 表单组件
│   ├── Field/
│   ├── Select/
│   ├── Checkbox/
│   ├── Radio/
│   ├── DatePicker/
│   └── ...
│
├── feedback/                # 反馈组件
│   ├── Toast/
│   ├── Loading/
│   ├── Skeleton/
│   ├── Empty/
│   └── ...
│
├── business/                # 业务组件
│   ├── CourseCard/
│   ├── SessionCard/
│   ├── StatCard/
│   ├── AchievementBadge/
│   └── ...
│
├── layout/                  # 布局组件
│   ├── AdminLayout/
│   ├── StudentLayout/
│   ├── Sidebar/
│   ├── Navbar/
│   └── ...
│
└── theme/                   # 主题专属组件
    ├── dark/
    ├── light/
    └── enterprise/
```

---

## 2. 基础组件

### 2.1 Button 按钮组件

#### 组件功能
- 多种变体：primary, secondary, ghost, danger
- 多种尺寸：sm, md, lg
- 支持图标
- 加载状态
- 禁用状态
- 块级按钮

#### 类型定义

```typescript
// components/base/Button/Button.types.ts

export type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'danger'
export type ButtonSize = 'sm' | 'md' | 'lg'

export interface ButtonProps {
  variant?: ButtonVariant
  size?: ButtonSize
  loading?: boolean
  disabled?: boolean
  block?: boolean
  icon?: string
  iconPosition?: 'left' | 'right'
  type?: 'button' | 'submit' | 'reset'
}
```

#### 组件实现

```vue
<!-- components/base/Button/Button.vue -->
<script setup lang="ts">
import { computed } from 'vue'
import type { ButtonProps, ButtonVariant, ButtonSize } from './Button.types'
import { Loader2 } from 'lucide-vue-next'

const props = withDefaults(defineProps<ButtonProps>(), {
  variant: 'primary',
  size: 'md',
  loading: false,
  disabled: false,
  block: false,
  iconPosition: 'left',
  type: 'button'
})

const emit = defineEmits<{
  (e: 'click', event: MouseEvent): void
}>()

const buttonClass = computed(() => [
  'base-button',
  `base-button--${props.variant}`,
  `base-button--${props.size}`,
  {
    'base-button--loading': props.loading,
    'base-button--disabled': props.disabled,
    'base-button--block': props.block,
    'base-button--icon-right': props.iconPosition === 'right'
  }
])

const handleClick = (e: MouseEvent) => {
  if (props.loading || props.disabled) return
  emit('click', e)
}
</script>

<template>
  <button
    :class="buttonClass"
    :type="type"
    :disabled="disabled || loading"
    @click="handleClick"
  >
    <!-- 加载图标 -->
    <Loader2
      v-if="loading"
      class="base-button__loader"
      :size="size === 'sm' ? 14 : size === 'lg' ? 20 : 16"
    />

    <!-- 左侧图标插槽 -->
    <span v-if="$slots.icon && iconPosition === 'left'" class="base-button__icon">
      <slot name="icon" />
    </span>

    <!-- 内容 -->
    <span class="base-button__content">
      <slot />
    </span>

    <!-- 右侧图标插槽 -->
    <span v-if="$slots.icon && iconPosition === 'right'" class="base-button__icon">
      <slot name="icon" />
    </span>
  </button>
</template>

<style scoped>
.base-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  font-weight: 500;
  cursor: pointer;
  transition: all var(--duration-fast, 200ms) var(--ease-out, ease-out);
  border: none;
  outline: none;
}

/* 尺寸变体 */
.base-button--sm {
  padding: 0.375rem 0.75rem;
  font-size: 0.875rem;
  border-radius: var(--radius-sm);
}

.base-button--md {
  padding: 0.5rem 1rem;
  font-size: 0.875rem;
  border-radius: var(--radius-sm);
}

.base-button--lg {
  padding: 0.75rem 1.5rem;
  font-size: 1rem;
  border-radius: var(--radius-sm);
}

/* Primary 变体 - 使用 CSS 变量适配三套主题 */
.base-button--primary {
  background: var(--accent);
  color: var(--accent-foreground, #fff);
  box-shadow: var(--shadow-button, none);
}

.base-button--primary:hover:not(:disabled) {
  background: var(--accent-bright, var(--accent));
  transform: translateY(-1px);
}

.base-button--primary:active:not(:disabled) {
  transform: scale(0.98);
}

/* Secondary 变体 */
.base-button--secondary {
  background: var(--surface, transparent);
  color: var(--foreground);
  border: 1px solid var(--border, var(--border-default));
}

.base-button--secondary:hover:not(:disabled) {
  background: var(--surface-hover, var(--muted));
  border-color: var(--border-hover, var(--border));
}

/* Ghost 变体 */
.base-button--ghost {
  background: transparent;
  color: var(--foreground-muted, var(--muted-foreground));
}

.base-button--ghost:hover:not(:disabled) {
  background: var(--surface, var(--muted));
  color: var(--foreground);
}

/* Danger 变体 */
.base-button--danger {
  background: #EF4444;
  color: #fff;
}

.base-button--danger:hover:not(:disabled) {
  background: #DC2626;
}

/* 状态 */
.base-button--disabled,
.base-button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.base-button--loading {
  cursor: wait;
}

.base-button--block {
  width: 100%;
}

/* 加载动画 */
.base-button__loader {
  animation: spin 1s linear infinite;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* Light 主题特殊处理 - 纯黑白、无圆角、即时响应、大写字母 */
[data-theme="light"] .base-button {
  border-radius: 0;
  transition: none;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  font-weight: 500;
  font-size: 0.875rem;
}

[data-theme="light"] .base-button--primary {
  background: #000000;
  color: #FFFFFF;
  border: none;
  box-shadow: none;
  padding: 1rem 2rem;
}

[data-theme="light"] .base-button--primary:hover:not(:disabled) {
  background: #FFFFFF;
  color: #000000;
  border: 2px solid #000000;
  transform: none;
}

[data-theme="light"] .base-button--secondary {
  background: transparent;
  color: #000000;
  border: 2px solid #000000;
}

[data-theme="light"] .base-button--secondary:hover:not(:disabled) {
  background: #000000;
  color: #FFFFFF;
}

[data-theme="light"] .base-button--ghost {
  background: transparent;
  color: #000000;
  border: none;
  text-decoration: underline;
  text-underline-offset: 4px;
}

[data-theme="light"] .base-button--ghost:hover:not(:disabled) {
  text-decoration-thickness: 2px;
}

/* Light 主题焦点状态 - 使用 focus-visible 避免鼠标点击时显示 */
[data-theme="light"] .base-button:focus-visible {
  outline: 3px solid #000000;
  outline-offset: 3px;
}
</style>
```

### 2.2 Card 卡片组件

#### 组件功能
- 基础卡片容器
- 可选头部、底部
- 悬停效果
- 点击交互
- 加载状态

#### 类型定义

```typescript
// components/base/Card/Card.types.ts

export type CardVariant = 'default' | 'glass' | 'gradient' | 'outline'

export interface CardProps {
  variant?: CardVariant
  hoverable?: boolean
  clickable?: boolean
  loading?: boolean
  padding?: 'none' | 'sm' | 'md' | 'lg'
}
```

#### 组件实现

```vue
<!-- components/base/Card/Card.vue -->
<script setup lang="ts">
import { computed } from 'vue'
import type { CardProps } from './Card.types'

const props = withDefaults(defineProps<CardProps>(), {
  variant: 'default',
  hoverable: false,
  clickable: false,
  loading: false,
  padding: 'md'
})

const emit = defineEmits<{
  (e: 'click', event: MouseEvent): void
}>()

const cardClass = computed(() => [
  'base-card',
  `base-card--${props.variant}`,
  `base-card--padding-${props.padding}`,
  {
    'base-card--hoverable': props.hoverable,
    'base-card--clickable': props.clickable,
    'base-card--loading': props.loading
  }
])
</script>

<template>
  <div
    :class="cardClass"
    :role="clickable ? 'button' : undefined"
    :tabindex="clickable ? 0 : undefined"
    @click="clickable && emit('click', $event)"
    @keydown.enter="clickable && emit('click', $event as any)"
  >
    <!-- 头部 -->
    <div v-if="$slots.header" class="base-card__header">
      <slot name="header" />
    </div>

    <!-- 内容 -->
    <div class="base-card__body">
      <slot />
    </div>

    <!-- 底部 -->
    <div v-if="$slots.footer" class="base-card__footer">
      <slot name="footer" />
    </div>

    <!-- 加载遮罩 -->
    <div v-if="loading" class="base-card__loading">
      <div class="base-card__spinner" />
    </div>
  </div>
</template>

<style scoped>
.base-card {
  position: relative;
  background: var(--surface, var(--foreground, #fff));
  border: 1px solid var(--border-default, var(--border));
  border-radius: var(--radius-lg, 16px);
  overflow: hidden;
  transition: all var(--duration-normal, 200ms) var(--ease-out, ease-out);
}

/* 内边距变体 */
.base-card--padding-none { padding: 0; }
.base-card--padding-sm { padding: 0.75rem; }
.base-card--padding-md { padding: 1.5rem; }
.base-card--padding-lg { padding: 2rem; }

/* 默认变体 */
.base-card--default {
  box-shadow: var(--shadow-card, none);
}

/* Glass 变体（Dark 主题特有） */
.base-card--glass {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(20px);
}

/* Gradient 变体 */
.base-card--gradient {
  background: linear-gradient(
    to bottom,
    rgba(255, 255, 255, 0.08),
    rgba(255, 255, 255, 0.02)
  );
}

/* Outline 变体 */
.base-card--outline {
  background: transparent;
  border-width: 2px;
}

/* 可悬停 */
.base-card--hoverable:hover {
  box-shadow: var(--shadow-card-hover, var(--shadow-card));
  transform: translateY(-2px);
}

/* 可点击 */
.base-card--clickable {
  cursor: pointer;
}

.base-card--clickable:active {
  transform: scale(0.99);
}

/* 区域 */
.base-card__header {
  padding-bottom: 1rem;
  border-bottom: 1px solid var(--border-default, var(--border));
  margin-bottom: 1rem;
}

.base-card__footer {
  padding-top: 1rem;
  border-top: 1px solid var(--border-default, var(--border));
  margin-top: 1rem;
}

/* 加载状态 */
.base-card__loading {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--surface, var(--background));
  opacity: 0.8;
}

.base-card__spinner {
  width: 24px;
  height: 24px;
  border: 2px solid var(--border);
  border-top-color: var(--accent);
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

/* Light 主题 - 无圆角、无阴影、即时响应 */
[data-theme="light"] .base-card {
  border-radius: 0;
  box-shadow: none;
  border: 1px solid var(--foreground);
  transition: none;  /* 即时响应，无过渡 */
}

[data-theme="light"] .base-card--hoverable:hover {
  box-shadow: none;
  transform: none;
  background: var(--foreground);
  color: var(--background);
  border-width: 2px;  /* hover时边框加粗 */
}

/* Light 主题 hover 时内部元素颜色反转 */
[data-theme="light"] .base-card--hoverable:hover .base-card__header,
[data-theme="light"] .base-card--hoverable:hover .base-card__body,
[data-theme="light"] .base-card--hoverable:hover .base-card__footer {
  color: var(--background);
  border-color: var(--background);
}

/* Light 主题焦点状态 */
[data-theme="light"] .base-card--clickable:focus-visible {
  outline: 3px solid #000000;
  outline-offset: 3px;
}
</style>
```

### 2.3 Table 表格组件

#### 组件功能
- 数据表格展示
- 排序功能
- 分页集成
- 行选择
- 自定义列渲染
- 空状态
- 加载状态

#### 类型定义

```typescript
// components/base/Table/Table.types.ts

export interface TableColumn<T = any> {
  key: string
  title: string
  width?: string | number
  align?: 'left' | 'center' | 'right'
  sortable?: boolean
  fixed?: 'left' | 'right'
  render?: (row: T, index: number) => any
}

export interface TableProps<T = any> {
  columns: TableColumn<T>[]
  data: T[]
  rowKey?: string | ((row: T) => string | number)
  loading?: boolean
  stripe?: boolean
  border?: boolean
  hoverable?: boolean
  selectable?: boolean
  selectedKeys?: (string | number)[]
  emptyText?: string
}

export interface TablePagination {
  page: number
  size: number
  total: number
  totalPages: number
}
```

#### 组件实现

```vue
<!-- components/base/Table/Table.vue -->
<script setup lang="ts" generic="T extends Record<string, any>">
import { computed, ref } from 'vue'
import type { TableColumn, TableProps, TablePagination } from './Table.types'
import { ChevronUp, ChevronDown } from 'lucide-vue-next'

const props = withDefaults(defineProps<TableProps<T>>(), {
  rowKey: 'id',
  loading: false,
  stripe: false,
  border: false,
  hoverable: true,
  selectable: false,
  emptyText: '暂无数据'
})

const emit = defineEmits<{
  (e: 'sort', key: string, order: 'asc' | 'desc'): void
  (e: 'select', selectedKeys: (string | number)[]): void
  (e: 'row-click', row: T, index: number): void
}>()

// 排序状态
const sortKey = ref<string>('')
const sortOrder = ref<'asc' | 'desc'>('asc')

// 获取行 key
const getRowKey = (row: T, index: number): string | number => {
  if (typeof props.rowKey === 'function') {
    return props.rowKey(row)
  }
  return row[props.rowKey] ?? index
}

// 处理排序
const handleSort = (column: TableColumn<T>) => {
  if (!column.sortable) return

  if (sortKey.value === column.key) {
    sortOrder.value = sortOrder.value === 'asc' ? 'desc' : 'asc'
  } else {
    sortKey.value = column.key
    sortOrder.value = 'asc'
  }

  emit('sort', sortKey.value, sortOrder.value)
}

// 处理行选择
const handleSelect = (key: string | number) => {
  if (!props.selectable) return

  const newKeys = [...(props.selectedKeys || [])]
  const index = newKeys.indexOf(key)

  if (index > -1) {
    newKeys.splice(index, 1)
  } else {
    newKeys.push(key)
  }

  emit('select', newKeys)
}

// 全选
const handleSelectAll = () => {
  if (!props.selectable) return

  const allKeys = props.data.map((row, i) => getRowKey(row, i))
  const allSelected = allKeys.every(k => props.selectedKeys?.includes(k))

  emit('select', allSelected ? [] : allKeys)
}

// 判断是否选中
const isSelected = (key: string | number) => {
  return props.selectedKeys?.includes(key)
}

// 表格类
const tableClass = computed(() => [
  'base-table',
  {
    'base-table--stripe': props.stripe,
    'base-table--border': props.border,
    'base-table--hoverable': props.hoverable,
    'base-table--loading': props.loading
  }
])
</script>

<template>
  <div class="base-table-wrapper">
    <table :class="tableClass">
      <!-- 表头 -->
      <thead class="base-table__head">
        <tr>
          <!-- 选择列 -->
          <th v-if="selectable" class="base-table__cell base-table__cell--checkbox">
            <input
              type="checkbox"
              :checked="selectedKeys?.length === data.length"
              :indeterminate="selectedKeys?.length > 0 && selectedKeys?.length < data.length"
              @change="handleSelectAll"
            />
          </th>

          <!-- 数据列 -->
          <th
            v-for="column in columns"
            :key="column.key"
            class="base-table__cell"
            :class="{
              'base-table__cell--sortable': column.sortable,
              'base-table__cell--sorted': sortKey === column.key
            }"
            :style="{
              width: column.width ? (typeof column.width === 'number' ? `${column.width}px` : column.width) : undefined,
              textAlign: column.align
            }"
            @click="handleSort(column)"
          >
            <span class="base-table__cell-content">
              {{ column.title }}
              <span v-if="column.sortable" class="base-table__sort-icon">
                <ChevronUp
                  :size="14"
                  :class="{ active: sortKey === column.key && sortOrder === 'asc' }"
                />
                <ChevronDown
                  :size="14"
                  :class="{ active: sortKey === column.key && sortOrder === 'desc' }"
                />
              </span>
            </span>
          </th>
        </tr>
      </thead>

      <!-- 表体 -->
      <tbody class="base-table__body">
        <tr
          v-for="(row, index) in data"
          :key="getRowKey(row, index)"
          class="base-table__row"
          :class="{ 'base-table__row--selected': isSelected(getRowKey(row, index)) }"
          @click="emit('row-click', row, index)"
        >
          <!-- 选择列 -->
          <td v-if="selectable" class="base-table__cell base-table__cell--checkbox">
            <input
              type="checkbox"
              :checked="isSelected(getRowKey(row, index))"
              @click.stop
              @change="handleSelect(getRowKey(row, index))"
            />
          </td>

          <!-- 数据列 -->
          <td
            v-for="column in columns"
            :key="column.key"
            class="base-table__cell"
            :style="{ textAlign: column.align }"
          >
            <!-- 自定义渲染 -->
            <template v-if="column.render">
              <component :is="column.render(row, index)" />
            </template>
            <!-- 插槽渲染 -->
            <template v-else-if="$slots[`cell-${column.key}`]">
              <slot :name="`cell-${column.key}`" :row="row" :index="index" />
            </template>
            <!-- 默认渲染 -->
            <template v-else>
              {{ row[column.key] }}
            </template>
          </td>
        </tr>

        <!-- 空状态 -->
        <tr v-if="data.length === 0 && !loading" class="base-table__empty">
          <td :colspan="columns.length + (selectable ? 1 : 0)">
            <slot name="empty">
              <div class="base-table__empty-content">
                {{ emptyText }}
              </div>
            </slot>
          </td>
        </tr>
      </tbody>
    </table>

    <!-- 加载状态 -->
    <div v-if="loading" class="base-table__loading">
      <div class="base-table__spinner" />
    </div>
  </div>
</template>

<style scoped>
.base-table-wrapper {
  position: relative;
  overflow-x: auto;
}

.base-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 0.875rem;
}

.base-table__head {
  background: var(--surface, var(--muted));
}

.base-table__cell {
  padding: 0.75rem 1rem;
  text-align: left;
  border-bottom: 1px solid var(--border-default, var(--border));
}

.base-table__head .base-table__cell {
  font-weight: 600;
  color: var(--foreground-muted, var(--muted-foreground));
}

.base-table__cell--sortable {
  cursor: pointer;
  user-select: none;
}

.base-table__cell--sortable:hover {
  color: var(--foreground);
}

.base-table__cell-content {
  display: flex;
  align-items: center;
  gap: 0.25rem;
}

.base-table__sort-icon {
  display: flex;
  flex-direction: column;
  gap: -4px;
  opacity: 0.3;
}

.base-table__sort-icon .active {
  opacity: 1;
  color: var(--accent);
}

/* 行样式 */
.base-table--stripe .base-table__row:nth-child(even) {
  background: var(--surface, var(--muted));
}

.base-table--border .base-table__cell {
  border: 1px solid var(--border-default, var(--border));
}

.base-table--hoverable .base-table__row:hover {
  background: var(--surface-hover, var(--muted));
}

.base-table__row--selected {
  background: rgba(94, 106, 210, 0.1) !important;
}

/* 空状态 */
.base-table__empty-content {
  padding: 2rem;
  text-align: center;
  color: var(--foreground-muted, var(--muted-foreground));
}

/* 加载状态 */
.base-table__loading {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.8);
}

[data-theme="dark"] .base-table__loading {
  background: rgba(5, 5, 6, 0.8);
}

.base-table__spinner {
  width: 32px;
  height: 32px;
  border: 3px solid var(--border);
  border-top-color: var(--accent);
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

/* Light 主题 - 无圆角、粗边框、即时响应 */
[data-theme="light"] .base-table {
  border: 2px solid var(--foreground);
}

[data-theme="light"] .base-table__cell {
  border-color: var(--foreground);
  transition: none;
}

[data-theme="light"] .base-table__head {
  background: var(--foreground);
  color: var(--background);
}

[data-theme="light"] .base-table__head .base-table__cell {
  color: var(--background);
  border-color: var(--background);
}

/* Light 主题行悬停 - 完全反色 */
[data-theme="light"] .base-table--hoverable .base-table__row:hover {
  background: var(--foreground);
  color: var(--background);
}

[data-theme="light"] .base-table--hoverable .base-table__row:hover .base-table__cell {
  color: var(--background);
  border-color: var(--background);
}

/* Light 主题选中行 */
[data-theme="light"] .base-table__row--selected {
  background: var(--foreground) !important;
  color: var(--background);
}

[data-theme="light"] .base-table__row--selected .base-table__cell {
  color: var(--background);
}

/* Light 主题焦点状态 */
[data-theme="light"] .base-table__row:focus-visible {
  outline: 3px solid #000000;
  outline-offset: -3px;
}

/* Light 主题复选框 */
[data-theme="light"] .base-table__cell--checkbox input[type="checkbox"] {
  appearance: none;
  width: 18px;
  height: 18px;
  border: 2px solid var(--foreground);
  background: transparent;
  cursor: pointer;
}

[data-theme="light"] .base-table__cell--checkbox input[type="checkbox"]:checked {
  background: var(--foreground);
}

[data-theme="light"] .base-table__cell--checkbox input[type="checkbox"]:checked::after {
  content: '✓';
  color: var(--background);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
}

/* Light 主题排序图标 */
[data-theme="light"] .base-table__sort-icon .active {
  color: var(--foreground);
}

/* Light 主题加载遮罩 */
[data-theme="light"] .base-table__loading {
  background: rgba(255, 255, 255, 0.9);
}

[data-theme="light"] .base-table__spinner {
  border-color: var(--foreground);
  border-top-color: transparent;
  border-radius: 0;
}
</style>
```

### 2.4 Pagination 分页组件

```vue
<!-- components/base/Pagination/Pagination.vue -->
<script setup lang="ts">
import { computed } from 'vue'
import { ChevronLeft, ChevronRight } from 'lucide-vue-next'

interface Props {
  page: number
  size: number
  total: number
  totalPages: number
  showSizeChanger?: boolean
  sizeOptions?: number[]
}

const props = withDefaults(defineProps<Props>(), {
  showSizeChanger: true,
  sizeOptions: () => [10, 20, 50, 100]
})

const emit = defineEmits<{
  (e: 'change', page: number): void
  (e: 'size-change', size: number): void
}>()

// 页码列表
const pageList = computed(() => {
  const pages: (number | string)[] = []
  const current = props.page
  const total = props.totalPages

  if (total <= 7) {
    for (let i = 1; i <= total; i++) pages.push(i)
  } else {
    pages.push(1)

    if (current > 3) pages.push('...')

    const start = Math.max(2, current - 1)
    const end = Math.min(total - 1, current + 1)

    for (let i = start; i <= end; i++) pages.push(i)

    if (current < total - 2) pages.push('...')

    pages.push(total)
  }

  return pages
})

const handlePageChange = (p: number | string) => {
  if (typeof p !== 'number') return
  if (p < 1 || p > props.totalPages) return
  emit('change', p)
}
</script>

<template>
  <div class="pagination">
    <!-- 每页条数 -->
    <div v-if="showSizeChanger" class="pagination__size">
      <span>每页</span>
      <select
        :value="size"
        @change="emit('size-change', Number(($event.target as HTMLSelectElement).value))"
      >
        <option v-for="s in sizeOptions" :key="s" :value="s">{{ s }}</option>
      </select>
      <span>条</span>
    </div>

    <!-- 总数 -->
    <div class="pagination__total">
      共 {{ total }} 条
    </div>

    <!-- 页码 -->
    <div class="pagination__pages">
      <!-- 上一页 -->
      <button
        class="pagination__btn"
        :disabled="page <= 1"
        @click="handlePageChange(page - 1)"
      >
        <ChevronLeft :size="16" />
      </button>

      <!-- 页码列表 -->
      <button
        v-for="(p, i) in pageList"
        :key="i"
        class="pagination__btn"
        :class="{ active: p === page, ellipsis: p === '...' }"
        :disabled="p === '...'"
        @click="handlePageChange(p)"
      >
        {{ p }}
      </button>

      <!-- 下一页 -->
      <button
        class="pagination__btn"
        :disabled="page >= totalPages"
        @click="handlePageChange(page + 1)"
      >
        <ChevronRight :size="16" />
      </button>
    </div>
  </div>
</template>

<style scoped>
.pagination {
  display: flex;
  align-items: center;
  gap: 1rem;
  font-size: 0.875rem;
}

.pagination__size {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.pagination__size select {
  padding: 0.25rem 0.5rem;
  border: 1px solid var(--border-default, var(--border));
  border-radius: var(--radius-sm, 4px);
  background: var(--surface, var(--background));
  color: var(--foreground);
}

.pagination__total {
  color: var(--foreground-muted, var(--muted-foreground));
}

.pagination__pages {
  display: flex;
  gap: 0.25rem;
}

.pagination__btn {
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 32px;
  height: 32px;
  padding: 0 0.5rem;
  border: 1px solid var(--border-default, var(--border));
  border-radius: var(--radius-sm, 4px);
  background: var(--surface, var(--background));
  color: var(--foreground);
  cursor: pointer;
  transition: all var(--duration-fast, 150ms);
}

.pagination__btn:hover:not(:disabled) {
  border-color: var(--accent);
  color: var(--accent);
}

.pagination__btn.active {
  background: var(--accent);
  border-color: var(--accent);
  color: var(--accent-foreground, #fff);
}

.pagination__btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.pagination__btn.ellipsis {
  border: none;
  background: transparent;
}

/* Light 主题 - 无圆角、即时响应、hover 反色 */
[data-theme="light"] .pagination__btn {
  border-radius: 0;
  border: 2px solid var(--foreground);
  transition: none;
}

[data-theme="light"] .pagination__btn:hover:not(:disabled) {
  background: var(--foreground);
  color: var(--background);
  border-color: var(--foreground);
}

[data-theme="light"] .pagination__btn.active {
  background: var(--foreground);
  color: var(--background);
  border-color: var(--foreground);
}

[data-theme="light"] .pagination__btn:focus-visible {
  outline: 3px solid #000000;
  outline-offset: 3px;
}

[data-theme="light"] .pagination__btn.ellipsis {
  border: none;
  background: transparent;
}

[data-theme="light"] .pagination__size select {
  border-radius: 0;
  border: 2px solid var(--foreground);
  transition: none;
}

[data-theme="light"] .pagination__size select:focus-visible {
  outline: 3px solid #000000;
  outline-offset: 3px;
}
</style>
```

---

## 3. 表单组件

### 3.1 Input 输入框组件

```vue
<!-- components/base/Input/Input.vue -->
<script setup lang="ts">
import { computed, ref } from 'vue'
import { Eye, EyeOff } from 'lucide-vue-next'

interface Props {
  modelValue?: string | number
  type?: 'text' | 'password' | 'email' | 'number' | 'tel' | 'url'
  label?: string
  placeholder?: string
  error?: string
  disabled?: boolean
  readonly?: boolean
  required?: boolean
  maxlength?: number
  autocomplete?: string
}

const props = withDefaults(defineProps<Props>(), {
  type: 'text',
  modelValue: ''
})

const emit = defineEmits<{
  (e: 'update:modelValue', value: string | number): void
  (e: 'focus', event: FocusEvent): void
  (e: 'blur', event: FocusEvent): void
}>()

const inputRef = ref<HTMLInputElement>()
const isFocused = ref(false)
const showPassword = ref(false)

const inputType = computed(() => {
  if (props.type === 'password') {
    return showPassword.value ? 'text' : 'password'
  }
  return props.type
})

const wrapperClass = computed(() => [
  'base-input',
  {
    'base-input--focused': isFocused.value,
    'base-input--error': props.error,
    'base-input--disabled': props.disabled
  }
])

const handleInput = (e: Event) => {
  const target = e.target as HTMLInputElement
  emit('update:modelValue', target.value)
}

const handleFocus = (e: FocusEvent) => {
  isFocused.value = true
  emit('focus', e)
}

const handleBlur = (e: FocusEvent) => {
  isFocused.value = false
  emit('blur', e)
}

const focus = () => inputRef.value?.focus()
const blur = () => inputRef.value?.blur()

defineExpose({ focus, blur })
</script>

<template>
  <div :class="wrapperClass">
    <!-- 标签 -->
    <label v-if="label" class="base-input__label">
      {{ label }}
      <span v-if="required" class="base-input__required">*</span>
    </label>

    <!-- 输入框容器 -->
    <div class="base-input__wrapper">
      <!-- 前缀插槽 -->
      <span v-if="$slots.prefix" class="base-input__prefix">
        <slot name="prefix" />
      </span>

      <!-- 输入框 -->
      <input
        ref="inputRef"
        :type="inputType"
        :value="modelValue"
        :placeholder="placeholder"
        :disabled="disabled"
        :readonly="readonly"
        :maxlength="maxlength"
        :autocomplete="autocomplete"
        class="base-input__input"
        @input="handleInput"
        @focus="handleFocus"
        @blur="handleBlur"
      />

      <!-- 密码显示切换 -->
      <button
        v-if="type === 'password'"
        type="button"
        class="base-input__toggle-password"
        @click="showPassword = !showPassword"
      >
        <Eye v-if="showPassword" :size="18" />
        <EyeOff v-else :size="18" />
      </button>

      <!-- 后缀插槽 -->
      <span v-if="$slots.suffix" class="base-input__suffix">
        <slot name="suffix" />
      </span>
    </div>

    <!-- 错误信息 -->
    <p v-if="error" class="base-input__error">
      {{ error }}
    </p>
  </div>
</template>

<style scoped>
.base-input {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.base-input__label {
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--foreground);
}

.base-input__required {
  color: #EF4444;
  margin-left: 0.25rem;
}

.base-input__wrapper {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 0.75rem;
  background: var(--surface, var(--background));
  border: 1px solid var(--border-default, var(--border));
  border-radius: var(--radius-sm);
  transition: all var(--duration-fast);
}

.base-input--focused .base-input__wrapper {
  border-color: var(--accent);
  box-shadow: var(--shadow-input-focus, 0 0 0 2px rgba(94, 106, 210, 0.2));
}

.base-input--error .base-input__wrapper {
  border-color: #EF4444;
}

.base-input--disabled .base-input__wrapper {
  opacity: 0.5;
  cursor: not-allowed;
}

.base-input__input {
  flex: 1;
  border: none;
  background: transparent;
  color: var(--foreground);
  font-size: 0.875rem;
  outline: none;
}

.base-input__input::placeholder {
  color: var(--foreground-subtle, var(--muted-foreground));
}

.base-input__input:disabled {
  cursor: not-allowed;
}

.base-input__prefix,
.base-input__suffix {
  display: flex;
  align-items: center;
  color: var(--foreground-muted, var(--muted-foreground));
}

.base-input__toggle-password {
  display: flex;
  align-items: center;
  padding: 0;
  border: none;
  background: transparent;
  color: var(--foreground-muted, var(--muted-foreground));
  cursor: pointer;
}

.base-input__error {
  font-size: 0.75rem;
  color: #EF4444;
  margin: 0;
}

/* Light 主题 - 底部边框风格、无圆角、即时响应 */
[data-theme="light"] .base-input__wrapper {
  border-radius: 0;
  border: none;
  border-bottom: 2px solid #000000;
  background: transparent;
  box-shadow: none;
  padding: 0.5rem 0;
}

[data-theme="light"] .base-input--focused .base-input__wrapper {
  border-bottom-width: 4px;
  box-shadow: none;
}

[data-theme="light"] .base-input__input::placeholder {
  color: #525252;
  font-style: italic;
}

[data-theme="light"] .base-input__label {
  text-transform: uppercase;
  letter-spacing: 0.1em;
  font-size: 0.75rem;
  font-weight: 500;
}

[data-theme="light"] .base-input:focus-visible {
  outline: none;
}
</style>
```

### 3.2 Select 选择器组件

```vue
<!-- components/base/Select/Select.vue -->
<script setup lang="ts">
import { ref, computed, watch, onMounted, onUnmounted } from 'vue'
import { ChevronDown, Check } from 'lucide-vue-next'

interface Option {
  value: string | number
  label: string
  disabled?: boolean
}

interface Props {
  modelValue?: string | number | null
  options: Option[]
  label?: string
  placeholder?: string
  error?: string
  disabled?: boolean
  clearable?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  placeholder: '请选择'
})

const emit = defineEmits<{
  (e: 'update:modelValue', value: string | number | null): void
  (e: 'change', value: string | number | null): void
}>()

const selectRef = ref<HTMLElement>()
const isOpen = ref(false)

const selectedOption = computed(() => {
  return props.options.find(opt => opt.value === props.modelValue)
})

const displayLabel = computed(() => {
  return selectedOption.value?.label || props.placeholder
})

const handleSelect = (option: Option) => {
  if (option.disabled) return
  emit('update:modelValue', option.value)
  emit('change', option.value)
  isOpen.value = false
}

const handleClear = (e: Event) => {
  e.stopPropagation()
  emit('update:modelValue', null)
  emit('change', null)
}

// 点击外部关闭
const handleClickOutside = (e: MouseEvent) => {
  if (selectRef.value && !selectRef.value.contains(e.target as Node)) {
    isOpen.value = false
  }
}

onMounted(() => {
  document.addEventListener('click', handleClickOutside)
})

onUnmounted(() => {
  document.removeEventListener('click', handleClickOutside)
})
</script>

<template>
  <div ref="selectRef" class="base-select" :class="{ 'base-select--open': isOpen, 'base-select--disabled': disabled }">
    <!-- 标签 -->
    <label v-if="label" class="base-select__label">{{ label }}</label>

    <!-- 触发器 -->
    <div
      class="base-select__trigger"
      :class="{ 'base-select__trigger--error': error }"
      @click="!disabled && (isOpen = !isOpen)"
    >
      <span :class="{ 'base-select__placeholder': !selectedOption }">
        {{ displayLabel }}
      </span>

      <div class="base-select__icons">
        <button
          v-if="clearable && selectedOption"
          type="button"
          class="base-select__clear"
          @click="handleClear"
        >
          ×
        </button>
        <ChevronDown
          :size="18"
          class="base-select__arrow"
          :class="{ 'base-select__arrow--up': isOpen }"
        />
      </div>
    </div>

    <!-- 下拉列表 -->
    <Transition name="dropdown">
      <div v-if="isOpen" class="base-select__dropdown">
        <div
          v-for="option in options"
          :key="option.value"
          class="base-select__option"
          :class="{
            'base-select__option--selected': option.value === modelValue,
            'base-select__option--disabled': option.disabled
          }"
          @click="handleSelect(option)"
        >
          <span>{{ option.label }}</span>
          <Check v-if="option.value === modelValue" :size="16" />
        </div>
      </div>
    </Transition>

    <!-- 错误信息 -->
    <p v-if="error" class="base-select__error">{{ error }}</p>
  </div>
</template>

<style scoped>
.base-select {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.base-select__label {
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--foreground);
}

.base-select__trigger {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.5rem 0.75rem;
  background: var(--surface, var(--background));
  border: 1px solid var(--border-default, var(--border));
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: all var(--duration-fast);
}

.base-select--open .base-select__trigger {
  border-color: var(--accent);
}

.base-select__trigger--error {
  border-color: #EF4444;
}

.base-select--disabled .base-select__trigger {
  opacity: 0.5;
  cursor: not-allowed;
}

.base-select__placeholder {
  color: var(--foreground-subtle, var(--muted-foreground));
}

.base-select__icons {
  display: flex;
  align-items: center;
  gap: 0.25rem;
}

.base-select__clear {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 18px;
  height: 18px;
  padding: 0;
  border: none;
  background: var(--surface-hover, var(--muted));
  border-radius: 50%;
  color: var(--foreground-muted);
  cursor: pointer;
}

.base-select__arrow {
  transition: transform var(--duration-fast);
  color: var(--foreground-muted);
}

.base-select__arrow--up {
  transform: rotate(180deg);
}

.base-select__dropdown {
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  margin-top: 0.25rem;
  padding: 0.25rem;
  background: var(--surface, var(--background));
  border: 1px solid var(--border-default, var(--border));
  border-radius: var(--radius-sm);
  box-shadow: var(--shadow-card);
  z-index: var(--z-dropdown);
  max-height: 200px;
  overflow-y: auto;
}

.base-select__option {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.5rem 0.75rem;
  border-radius: calc(var(--radius-sm) - 2px);
  cursor: pointer;
  transition: background var(--duration-fast);
}

.base-select__option:hover:not(.base-select__option--disabled) {
  background: var(--surface-hover, var(--muted));
}

.base-select__option--selected {
  color: var(--accent);
}

.base-select__option--disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.base-select__error {
  font-size: 0.75rem;
  color: #EF4444;
  margin: 0;
}

/* 下拉动画 */
.dropdown-enter-active,
.dropdown-leave-active {
  transition: all var(--duration-fast);
}

.dropdown-enter-from,
.dropdown-leave-to {
  opacity: 0;
  transform: translateY(-8px);
}

/* Light 主题 - 无圆角、即时响应、hover 反色 */
[data-theme="light"] .base-select__trigger,
[data-theme="light"] .base-select__dropdown {
  border-radius: 0;
  border-width: 2px;
  border-color: var(--foreground);
  transition: none;
  box-shadow: none;
}

[data-theme="light"] .base-select__trigger:hover {
  background: var(--foreground);
  color: var(--background);
}

[data-theme="light"] .base-select__trigger:hover .base-select__placeholder,
[data-theme="light"] .base-select__trigger:hover .base-select__arrow {
  color: var(--background);
}

/* Light 主题打开状态 */
[data-theme="light"] .base-select--open .base-select__trigger {
  background: var(--foreground);
  color: var(--background);
  border-bottom-width: 4px;
}

[data-theme="light"] .base-select--open .base-select__trigger .base-select__arrow,
[data-theme="light"] .base-select--open .base-select__trigger .base-select__placeholder {
  color: var(--background);
}

/* Light 主题选项 */
[data-theme="light"] .base-select__option {
  border-radius: 0;
  transition: none;
}

[data-theme="light"] .base-select__option:hover:not(.base-select__option--disabled) {
  background: var(--foreground);
  color: var(--background);
}

[data-theme="light"] .base-select__option--selected {
  background: var(--foreground);
  color: var(--background);
  font-weight: 500;
}

/* Light 主题清除按钮 */
[data-theme="light"] .base-select__clear {
  border-radius: 0;
  border: 1px solid var(--foreground);
  background: transparent;
}

[data-theme="light"] .base-select__clear:hover {
  background: var(--foreground);
  color: var(--background);
}

/* Light 主题箭头无动画 */
[data-theme="light"] .base-select__arrow {
  transition: none;
}

/* Light 主题焦点状态 */
[data-theme="light"] .base-select__trigger:focus-visible {
  outline: 3px solid #000000;
  outline-offset: 3px;
}

/* Light 主题下拉动画 - 即时出现 */
[data-theme="light"] .dropdown-enter-active,
[data-theme="light"] .dropdown-leave-active {
  transition: none;
}

[data-theme="light"] .dropdown-enter-from,
[data-theme="light"] .dropdown-leave-to {
  opacity: 1;
  transform: none;
}

/* Light 主题标签大写 */
[data-theme="light"] .base-select__label {
  text-transform: uppercase;
  letter-spacing: 0.1em;
  font-size: 0.75rem;
  font-weight: 500;
}
</style>
```

---

## 4. 反馈组件

### 4.1 Toast 轻提示组件

```vue
<!-- components/feedback/Toast/Toast.vue -->
<script setup lang="ts">
import { CheckCircle, XCircle, AlertTriangle, Info, X } from 'lucide-vue-next'

interface ToastItem {
  id: string
  type: 'success' | 'error' | 'warning' | 'info'
  title: string
  message?: string
}

defineProps<{
  items: ToastItem[]
}>()

const emit = defineEmits<{
  (e: 'close', id: string): void
}>()

const iconMap = {
  success: CheckCircle,
  error: XCircle,
  warning: AlertTriangle,
  info: Info
}
</script>

<template>
  <Teleport to="body">
    <div class="toast-container">
      <TransitionGroup name="toast">
        <div
          v-for="item in items"
          :key="item.id"
          class="toast"
          :class="`toast--${item.type}`"
        >
          <component :is="iconMap[item.type]" :size="20" class="toast__icon" />
          <div class="toast__content">
            <p class="toast__title">{{ item.title }}</p>
            <p v-if="item.message" class="toast__message">{{ item.message }}</p>
          </div>
          <button class="toast__close" @click="emit('close', item.id)">
            <X :size="16" />
          </button>
        </div>
      </TransitionGroup>
    </div>
  </Teleport>
</template>

<style scoped>
.toast-container {
  position: fixed;
  top: 1rem;
  right: 1rem;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  z-index: var(--z-toast, 1400);
}

.toast {
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
  padding: 1rem;
  min-width: 300px;
  max-width: 400px;
  background: var(--surface, var(--background));
  border: 1px solid var(--border-default, var(--border));
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
}

.toast--success .toast__icon { color: #10B981; }
.toast--error .toast__icon { color: #EF4444; }
.toast--warning .toast__icon { color: #F59E0B; }
.toast--info .toast__icon { color: #3B82F6; }

.toast__content {
  flex: 1;
}

.toast__title {
  margin: 0;
  font-weight: 500;
  color: var(--foreground);
}

.toast__message {
  margin: 0.25rem 0 0;
  font-size: 0.875rem;
  color: var(--foreground-muted, var(--muted-foreground));
}

.toast__close {
  display: flex;
  padding: 0;
  border: none;
  background: transparent;
  color: var(--foreground-muted);
  cursor: pointer;
}

/* 动画 */
.toast-enter-active,
.toast-leave-active {
  transition: all var(--duration-normal);
}

.toast-enter-from {
  opacity: 0;
  transform: translateX(100%);
}

.toast-leave-to {
  opacity: 0;
  transform: translateX(100%);
}

/* Light 主题 - 无圆角、即时响应 */
[data-theme="light"] .toast {
  border-radius: 0;
  border-width: 2px;
  border-color: var(--foreground);
  box-shadow: none;
  transition: none;
}

/* Light 主题关闭按钮 */
[data-theme="light"] .toast__close {
  border-radius: 0;
  transition: none;
}

[data-theme="light"] .toast__close:hover {
  background: var(--foreground);
  color: var(--background);
}

[data-theme="light"] .toast__close:focus-visible {
  outline: 3px solid #000000;
  outline-offset: 3px;
}

/* Light 主题图标颜色保持功能色（success/error/warning/info 需要区分） */
/* 但边框统一使用黑色 */

/* Light 主题动画 - 即时出现/消失 */
[data-theme="light"] .toast-enter-active,
[data-theme="light"] .toast-leave-active {
  transition: none;
}

[data-theme="light"] .toast-enter-from,
[data-theme="light"] .toast-leave-to {
  opacity: 1;
  transform: none;
}
</style>
```

### 4.2 Modal 弹窗组件

```vue
<!-- components/base/Modal/Modal.vue -->
<script setup lang="ts">
import { watch, onMounted, onUnmounted } from 'vue'
import { X } from 'lucide-vue-next'

interface Props {
  modelValue: boolean
  title?: string
  width?: string
  closable?: boolean
  maskClosable?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  width: '500px',
  closable: true,
  maskClosable: true
})

const emit = defineEmits<{
  (e: 'update:modelValue', value: boolean): void
  (e: 'close'): void
}>()

const close = () => {
  emit('update:modelValue', false)
  emit('close')
}

const handleMaskClick = () => {
  if (props.maskClosable) close()
}

// ESC 关闭
const handleKeydown = (e: KeyboardEvent) => {
  if (e.key === 'Escape' && props.modelValue && props.closable) {
    close()
  }
}

// 禁止背景滚动
watch(() => props.modelValue, (val) => {
  document.body.style.overflow = val ? 'hidden' : ''
})

onMounted(() => {
  document.addEventListener('keydown', handleKeydown)
})

onUnmounted(() => {
  document.removeEventListener('keydown', handleKeydown)
  document.body.style.overflow = ''
})
</script>

<template>
  <Teleport to="body">
    <Transition name="modal">
      <div v-if="modelValue" class="modal-overlay" @click.self="handleMaskClick">
        <div class="modal" :style="{ width }">
          <!-- 头部 -->
          <div v-if="title || closable" class="modal__header">
            <h3 v-if="title" class="modal__title">{{ title }}</h3>
            <button v-if="closable" class="modal__close" @click="close">
              <X :size="20" />
            </button>
          </div>

          <!-- 内容 -->
          <div class="modal__body">
            <slot />
          </div>

          <!-- 底部 -->
          <div v-if="$slots.footer" class="modal__footer">
            <slot name="footer" />
          </div>
        </div>
      </div>
    </Transition>
  </Teleport>
</template>

<style scoped>
.modal-overlay {
  position: fixed;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 2rem;
  background: rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(4px);
  z-index: var(--z-modal, 1200);
}

.modal {
  background: var(--surface, var(--background));
  border: 1px solid var(--border-default, var(--border));
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-card);
  max-height: calc(100vh - 4rem);
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.modal__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1rem 1.5rem;
  border-bottom: 1px solid var(--border-default, var(--border));
}

.modal__title {
  margin: 0;
  font-size: 1.125rem;
  font-weight: 600;
  color: var(--foreground);
}

.modal__close {
  display: flex;
  padding: 0.25rem;
  border: none;
  background: transparent;
  color: var(--foreground-muted);
  cursor: pointer;
  border-radius: var(--radius-sm);
  transition: all var(--duration-fast);
}

.modal__close:hover {
  background: var(--surface-hover, var(--muted));
  color: var(--foreground);
}

.modal__body {
  flex: 1;
  padding: 1.5rem;
  overflow-y: auto;
}

.modal__footer {
  display: flex;
  justify-content: flex-end;
  gap: 0.75rem;
  padding: 1rem 1.5rem;
  border-top: 1px solid var(--border-default, var(--border));
}

/* 动画 */
.modal-enter-active,
.modal-leave-active {
  transition: all var(--duration-normal);
}

.modal-enter-active .modal,
.modal-leave-active .modal {
  transition: all var(--duration-normal);
}

.modal-enter-from,
.modal-leave-to {
  opacity: 0;
}

.modal-enter-from .modal,
.modal-leave-to .modal {
  transform: scale(0.95);
}

/* Light 主题 - 无圆角、无阴影、即时响应 */
[data-theme="light"] .modal-overlay {
  backdrop-filter: none;  /* 无模糊效果 */
  background: rgba(255, 255, 255, 0.9);  /* 白色半透明遮罩 */
}

[data-theme="light"] .modal {
  border-radius: 0;
  border-width: 2px;
  border-color: var(--foreground);
  box-shadow: none;
}

[data-theme="light"] .modal__header {
  border-bottom-width: 2px;
  border-color: var(--foreground);
}

[data-theme="light"] .modal__title {
  text-transform: uppercase;
  letter-spacing: 0.05em;
  font-weight: 500;
}

[data-theme="light"] .modal__footer {
  border-top-width: 2px;
  border-color: var(--foreground);
}

[data-theme="light"] .modal__close {
  border-radius: 0;
  transition: none;
}

[data-theme="light"] .modal__close:hover {
  background: var(--foreground);
  color: var(--background);
}

[data-theme="light"] .modal__close:focus-visible {
  outline: 3px solid #000000;
  outline-offset: 3px;
}

/* Light 主题动画 - 即时出现/消失 */
[data-theme="light"] .modal-enter-active,
[data-theme="light"] .modal-leave-active {
  transition: none;
}

[data-theme="light"] .modal-enter-active .modal,
[data-theme="light"] .modal-leave-active .modal {
  transition: none;
}

[data-theme="light"] .modal-enter-from,
[data-theme="light"] .modal-leave-to {
  opacity: 1;
}

[data-theme="light"] .modal-enter-from .modal,
[data-theme="light"] .modal-leave-to .modal {
  transform: none;
}
</style>
```

### 4.3 Skeleton 骨架屏组件

```vue
<!-- components/feedback/Skeleton/Skeleton.vue -->
<script setup lang="ts">
interface Props {
  width?: string
  height?: string
  circle?: boolean
  rows?: number
  animated?: boolean
}

withDefaults(defineProps<Props>(), {
  width: '100%',
  height: '1rem',
  circle: false,
  rows: 1,
  animated: true
})
</script>

<template>
  <div class="skeleton-wrapper">
    <div
      v-for="i in rows"
      :key="i"
      class="skeleton"
      :class="{ 'skeleton--circle': circle, 'skeleton--animated': animated }"
      :style="{
        width: circle ? height : width,
        height,
        borderRadius: circle ? '50%' : undefined
      }"
    />
  </div>
</template>

<style scoped>
.skeleton-wrapper {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.skeleton {
  background: var(--surface, var(--muted));
  border-radius: var(--radius-sm);
}

.skeleton--animated {
  animation: skeleton-pulse 1.5s ease-in-out infinite;
}

@keyframes skeleton-pulse {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.5;
  }
}

/* Light 主题 */
[data-theme="light"] .skeleton {
  border-radius: 0;
  border: 1px solid var(--border);
}
</style>
```

---

## 5. 业务组件

### 5.1 CourseCard 课程卡片组件

```vue
<!-- components/business/CourseCard/CourseCard.vue -->
<script setup lang="ts">
import { computed } from 'vue'
import { useRouter } from 'vue-router'
import { BookOpen, Clock, Award } from 'lucide-vue-next'
import type { Course, CourseLevel } from '@/types/course'
import BaseCard from '@/components/base/Card/Card.vue'
import BaseButton from '@/components/base/Button/Button.vue'

interface Props {
  course: Course
  showActions?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  showActions: true
})

const emit = defineEmits<{
  (e: 'view', course: Course): void
  (e: 'enroll', course: Course): void
}>()

const router = useRouter()

const levelLabel = computed(() => {
  const map: Record<CourseLevel, string> = {
    BEGINNER: '入门',
    INTERMEDIATE: '进阶',
    ADVANCED: '高级'
  }
  return map[props.course.level]
})

const levelClass = computed(() => {
  const map: Record<CourseLevel, string> = {
    BEGINNER: 'level--beginner',
    INTERMEDIATE: 'level--intermediate',
    ADVANCED: 'level--advanced'
  }
  return map[props.course.level]
})

const handleView = () => {
  emit('view', props.course)
  router.push(`/student/courses/${props.course.id}`)
}
</script>

<template>
  <BaseCard class="course-card" hoverable clickable @click="handleView">
    <!-- 封面图 -->
    <div class="course-card__cover">
      <img
        v-if="course.coverImage"
        :src="course.coverImage"
        :alt="course.name"
      />
      <div v-else class="course-card__cover-placeholder">
        <BookOpen :size="48" />
      </div>

      <!-- 难度标签 -->
      <span class="course-card__level" :class="levelClass">
        {{ levelLabel }}
      </span>
    </div>

    <!-- 内容 -->
    <div class="course-card__content">
      <h3 class="course-card__title">{{ course.name }}</h3>
      <p class="course-card__category">{{ course.category }}</p>
      <p class="course-card__description">{{ course.description }}</p>

      <!-- 元信息 -->
      <div class="course-card__meta">
        <span class="course-card__meta-item">
          <Clock :size="14" />
          {{ course.duration }} 课时
        </span>
        <span class="course-card__meta-item">
          <Award :size="14" />
          {{ course.credits }} 学分
        </span>
      </div>

      <!-- 操作按钮 -->
      <div v-if="showActions" class="course-card__actions">
        <BaseButton size="sm" @click.stop="handleView">
          查看详情
        </BaseButton>
        <BaseButton
          variant="primary"
          size="sm"
          @click.stop="emit('enroll', course)"
        >
          立即报名
        </BaseButton>
      </div>
    </div>
  </BaseCard>
</template>

<style scoped>
.course-card {
  display: flex;
  flex-direction: column;
}

.course-card__cover {
  position: relative;
  aspect-ratio: 16 / 9;
  overflow: hidden;
}

.course-card__cover img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform var(--duration-normal);
}

.course-card:hover .course-card__cover img {
  transform: scale(1.05);
}

.course-card__cover-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;
  background: var(--surface, var(--muted));
  color: var(--foreground-muted);
}

.course-card__level {
  position: absolute;
  top: 0.75rem;
  right: 0.75rem;
  padding: 0.25rem 0.5rem;
  font-size: 0.75rem;
  font-weight: 500;
  border-radius: var(--radius-sm);
}

.level--beginner {
  background: #10B981;
  color: #fff;
}

.level--intermediate {
  background: #F59E0B;
  color: #fff;
}

.level--advanced {
  background: #EF4444;
  color: #fff;
}

.course-card__content {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  padding: 1rem;
}

.course-card__title {
  margin: 0;
  font-size: 1rem;
  font-weight: 600;
  color: var(--foreground);
  line-height: 1.4;
}

.course-card__category {
  margin: 0;
  font-size: 0.75rem;
  color: var(--accent);
}

.course-card__description {
  margin: 0;
  font-size: 0.875rem;
  color: var(--foreground-muted, var(--muted-foreground));
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.course-card__meta {
  display: flex;
  gap: 1rem;
  font-size: 0.75rem;
  color: var(--foreground-muted, var(--muted-foreground));
}

.course-card__meta-item {
  display: flex;
  align-items: center;
  gap: 0.25rem;
}

.course-card__actions {
  display: flex;
  gap: 0.5rem;
  margin-top: 0.5rem;
}

/* Light 主题 */
[data-theme="light"] .course-card__level {
  border-radius: 0;
}
</style>
```

### 5.2 StatCard 统计卡片组件

```vue
<!-- components/business/StatCard/StatCard.vue -->
<script setup lang="ts">
import { computed } from 'vue'
import {
  Users,
  BookOpen,
  Calendar,
  ClipboardList,
  TrendingUp,
  TrendingDown
} from 'lucide-vue-next'
import BaseCard from '@/components/base/Card/Card.vue'

interface Props {
  title: string
  value: number | string
  icon: string
  trend?: string
  trendType?: 'up' | 'down' | 'neutral'
}

const props = defineProps<Props>()

const iconMap: Record<string, any> = {
  Users,
  BookOpen,
  Calendar,
  ClipboardList
}

const IconComponent = computed(() => iconMap[props.icon] || Users)
</script>

<template>
  <BaseCard class="stat-card" hoverable>
    <div class="stat-card__content">
      <div class="stat-card__info">
        <p class="stat-card__title">{{ title }}</p>
        <p class="stat-card__value">{{ value }}</p>

        <p v-if="trend" class="stat-card__trend" :class="`stat-card__trend--${trendType}`">
          <TrendingUp v-if="trendType === 'up'" :size="14" />
          <TrendingDown v-if="trendType === 'down'" :size="14" />
          {{ trend }}
        </p>
      </div>

      <div class="stat-card__icon">
        <component :is="IconComponent" :size="24" />
      </div>
    </div>
  </BaseCard>
</template>

<style scoped>
.stat-card__content {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}

.stat-card__info {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.stat-card__title {
  margin: 0;
  font-size: 0.875rem;
  color: var(--foreground-muted, var(--muted-foreground));
}

.stat-card__value {
  margin: 0;
  font-size: 1.75rem;
  font-weight: 700;
  color: var(--foreground);
}

.stat-card__trend {
  display: flex;
  align-items: center;
  gap: 0.25rem;
  margin: 0;
  font-size: 0.75rem;
}

.stat-card__trend--up {
  color: #10B981;
}

.stat-card__trend--down {
  color: #EF4444;
}

.stat-card__trend--neutral {
  color: var(--foreground-muted);
}

.stat-card__icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 48px;
  height: 48px;
  background: var(--surface, var(--muted));
  border-radius: var(--radius-md);
  color: var(--accent);
}

/* Light 主题 */
[data-theme="light"] .stat-card__icon {
  border-radius: 0;
  border: 2px solid var(--foreground);
  background: transparent;
  color: var(--foreground);
}
</style>
```

### 5.3 AchievementBadge 成就徽章组件

```vue
<!-- components/business/AchievementBadge/AchievementBadge.vue -->
<script setup lang="ts">
import { computed } from 'vue'
import { Trophy, Star, Zap, Crown } from 'lucide-vue-next'
import type { Achievement } from '@/types/learning'

interface Props {
  achievement: Achievement
  unlocked?: boolean
  size?: 'sm' | 'md' | 'lg'
}

const props = withDefaults(defineProps<Props>(), {
  unlocked: false,
  size: 'md'
})

const iconMap: Record<string, any> = {
  trophy: Trophy,
  star: Star,
  zap: Zap,
  crown: Crown
}

const IconComponent = computed(() => {
  return iconMap[props.achievement.icon] || Trophy
})

const rarityClass = computed(() => {
  return `achievement--${props.achievement.rarity.toLowerCase()}`
})

const sizeClass = computed(() => {
  return `achievement--${props.size}`
})
</script>

<template>
  <div
    class="achievement-badge"
    :class="[rarityClass, sizeClass, { 'achievement--locked': !unlocked }]"
    :title="achievement.name"
  >
    <div class="achievement-badge__icon">
      <component :is="IconComponent" />
    </div>
    <div class="achievement-badge__info">
      <span class="achievement-badge__name">{{ achievement.name }}</span>
      <span class="achievement-badge__points">{{ achievement.points }} 积分</span>
    </div>

    <!-- 锁定遮罩 -->
    <div v-if="!unlocked" class="achievement-badge__lock">
      🔒
    </div>
  </div>
</template>

<style scoped>
.achievement-badge {
  position: relative;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.75rem;
  background: var(--surface, var(--background));
  border: 2px solid var(--border);
  border-radius: var(--radius-md);
  transition: all var(--duration-fast);
}

/* 尺寸 */
.achievement--sm {
  padding: 0.5rem;
  gap: 0.5rem;
}

.achievement--sm .achievement-badge__icon {
  width: 32px;
  height: 32px;
}

.achievement--lg {
  padding: 1rem;
  gap: 1rem;
}

.achievement--lg .achievement-badge__icon {
  width: 56px;
  height: 56px;
}

/* 稀有度颜色 */
.achievement--common {
  border-color: #9CA3AF;
}

.achievement--common .achievement-badge__icon {
  background: #F3F4F6;
  color: #6B7280;
}

.achievement--rare {
  border-color: #3B82F6;
}

.achievement--rare .achievement-badge__icon {
  background: linear-gradient(135deg, #3B82F6, #1D4ED8);
  color: #fff;
}

.achievement--epic {
  border-color: #8B5CF6;
}

.achievement--epic .achievement-badge__icon {
  background: linear-gradient(135deg, #8B5CF6, #6D28D9);
  color: #fff;
}

.achievement--legendary {
  border-color: #F59E0B;
}

.achievement--legendary .achievement-badge__icon {
  background: linear-gradient(135deg, #F59E0B, #D97706);
  color: #fff;
}

.achievement-badge__icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 44px;
  height: 44px;
  border-radius: 50%;
}

.achievement-badge__info {
  display: flex;
  flex-direction: column;
  gap: 0.125rem;
}

.achievement-badge__name {
  font-weight: 600;
  color: var(--foreground);
}

.achievement-badge__points {
  font-size: 0.75rem;
  color: var(--foreground-muted, var(--muted-foreground));
}

/* 锁定状态 */
.achievement--locked {
  opacity: 0.6;
  filter: grayscale(50%);
}

.achievement-badge__lock {
  position: absolute;
  top: -8px;
  right: -8px;
  font-size: 1rem;
}

/* Light 主题 */
[data-theme="light"] .achievement-badge {
  border-radius: 0;
}

[data-theme="light"] .achievement-badge__icon {
  border-radius: 0;
}
</style>
```

---

## 6. 布局组件

（布局组件已在 04-页面模块拆分.md 中详细说明，此处省略）

---

## 7. Light 主题专属组件

> Light 主题遵循极简单色设计哲学，以下组件专为该主题设计，体现"还原本质"的设计理念。

### 7.1 SectionDivider 区块分隔线

#### 设计哲学
在没有阴影和渐变的世界里，线条成为定义空间的唯一工具。粗细变化传达层级，方向变化引导视线。

#### 组件功能
- 多种粗细：hairline（1px）、thin（2px）、medium（4px）、thick（8px）
- 支持水平/垂直方向
- 可选渐变淡出效果
- 支持标签文字

#### 类型定义

```typescript
// components/theme/light/SectionDivider/SectionDivider.types.ts

export type DividerWeight = 'hairline' | 'thin' | 'medium' | 'thick'
export type DividerDirection = 'horizontal' | 'vertical'

export interface SectionDividerProps {
  weight?: DividerWeight
  direction?: DividerDirection
  fade?: boolean
  label?: string
  spacing?: 'sm' | 'md' | 'lg' | 'xl'
}
```

#### 组件实现

```vue
<!-- components/theme/light/SectionDivider/SectionDivider.vue -->
<script setup lang="ts">
import { computed } from 'vue'
import type { SectionDividerProps } from './SectionDivider.types'

const props = withDefaults(defineProps<SectionDividerProps>(), {
  weight: 'medium',
  direction: 'horizontal',
  fade: false,
  spacing: 'lg'
})

const weightMap = {
  hairline: '1px',
  thin: '2px',
  medium: '4px',
  thick: '8px'
}

const spacingMap = {
  sm: '1rem',
  md: '2rem',
  lg: '4rem',
  xl: '6rem'
}

const dividerStyle = computed(() => {
  const size = weightMap[props.weight]
  const space = spacingMap[props.spacing]

  if (props.direction === 'vertical') {
    return {
      width: size,
      height: '100%',
      marginLeft: space,
      marginRight: space
    }
  }
  return {
    height: size,
    width: '100%',
    marginTop: space,
    marginBottom: space
  }
})

const dividerClass = computed(() => [
  'section-divider',
  `section-divider--${props.direction}`,
  `section-divider--${props.weight}`,
  { 'section-divider--fade': props.fade }
])
</script>

<template>
  <div :class="dividerClass" :style="dividerStyle" role="separator">
    <!-- 带标签的分隔线 -->
    <template v-if="label">
      <span class="section-divider__line section-divider__line--left" />
      <span class="section-divider__label">{{ label }}</span>
      <span class="section-divider__line section-divider__line--right" />
    </template>
  </div>
</template>

<style scoped>
.section-divider {
  display: flex;
  align-items: center;
  background: #000000;
}

/* 带标签时变为flex容器 */
.section-divider:has(.section-divider__label) {
  background: transparent;
  gap: 1.5rem;
}

.section-divider__line {
  flex: 1;
  height: inherit;
  background: #000000;
}

.section-divider__label {
  font-family: var(--font-body, 'Source Serif 4', Georgia, serif);
  font-size: 0.75rem;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.2em;
  color: #000000;
  white-space: nowrap;
}

/* 渐变淡出效果 */
.section-divider--fade.section-divider--horizontal {
  background: linear-gradient(
    to right,
    transparent 0%,
    #000000 15%,
    #000000 85%,
    transparent 100%
  );
}

.section-divider--fade.section-divider--vertical {
  background: linear-gradient(
    to bottom,
    transparent 0%,
    #000000 15%,
    #000000 85%,
    transparent 100%
  );
}

/* 垂直方向 */
.section-divider--vertical {
  flex-direction: column;
}

.section-divider--vertical .section-divider__line {
  width: inherit;
  height: auto;
  flex: 1;
}

.section-divider--vertical .section-divider__label {
  writing-mode: vertical-rl;
  text-orientation: mixed;
}
</style>
```

#### 使用示例

```vue
<!-- 基础分隔线 -->
<SectionDivider />

<!-- 粗分隔线 -->
<SectionDivider weight="thick" />

<!-- 带标签的分隔线 -->
<SectionDivider weight="medium" label="继续阅读" />

<!-- 渐变淡出 -->
<SectionDivider weight="thin" fade />

<!-- 垂直分隔线 -->
<SectionDivider direction="vertical" weight="medium" />
```

### 7.2 EditorialQuote 编辑引言组件

#### 设计哲学
在极简主义设计中，引用成为页面的戏剧性停顿。超大字号的斜体衬线字体与标志性的引号装饰创造视觉锚点，打破连续阅读的节奏，强调核心观点。

#### 组件功能
- 超大斜体衬线字体（Playfair Display Italic）
- 装饰性超大引号（纯排版符号，非 emoji）
- 引言归属行（小型大写字母）
- 可选边框装饰
- 响应式字号缩放

#### 类型定义

```typescript
// components/theme/light/EditorialQuote/EditorialQuote.types.ts

export type QuoteStyle = 'default' | 'bordered' | 'indented'

export interface EditorialQuoteProps {
  quote: string
  author?: string
  source?: string
  style?: QuoteStyle
  size?: 'md' | 'lg' | 'xl'
}
```

#### 组件实现

```vue
<!-- components/theme/light/EditorialQuote/EditorialQuote.vue -->
<script setup lang="ts">
import { computed } from 'vue'
import type { EditorialQuoteProps } from './EditorialQuote.types'

const props = withDefaults(defineProps<EditorialQuoteProps>(), {
  style: 'default',
  size: 'lg'
})

const sizeMap = {
  md: 'text-2xl',
  lg: 'text-4xl',
  xl: 'text-6xl'
}

const quoteClass = computed(() => [
  'editorial-quote',
  `editorial-quote--${props.style}`,
  `editorial-quote--${props.size}`
])
</script>

<template>
  <blockquote :class="quoteClass">
    <!-- 开引号装饰 -->
    <span class="editorial-quote__mark editorial-quote__mark--open" aria-hidden="true">"</span>

    <!-- 引言内容 -->
    <p class="editorial-quote__text">
      {{ quote }}
    </p>

    <!-- 闭引号装饰 -->
    <span class="editorial-quote__mark editorial-quote__mark--close" aria-hidden="true">"</span>

    <!-- 归属信息 -->
    <footer v-if="author || source" class="editorial-quote__attribution">
      <span v-if="author" class="editorial-quote__author">{{ author }}</span>
      <span v-if="author && source" class="editorial-quote__separator">—</span>
      <cite v-if="source" class="editorial-quote__source">{{ source }}</cite>
    </footer>
  </blockquote>
</template>

<style scoped>
.editorial-quote {
  position: relative;
  margin: 4rem 0;
  padding: 0;
  text-align: center;
}

/* 引言文字 - 大号斜体衬线 */
.editorial-quote__text {
  font-family: 'Playfair Display', Georgia, serif;
  font-style: italic;
  font-weight: 400;
  color: #000000;
  line-height: 1.3;
  margin: 0;
  padding: 0 3rem;
}

/* 尺寸变体 */
.editorial-quote--md .editorial-quote__text {
  font-size: 1.5rem;
}

.editorial-quote--lg .editorial-quote__text {
  font-size: 2.5rem;
}

.editorial-quote--xl .editorial-quote__text {
  font-size: 4rem;
  letter-spacing: -0.02em;
}

/* 超大装饰引号 */
.editorial-quote__mark {
  position: absolute;
  font-family: 'Playfair Display', Georgia, serif;
  font-size: 8rem;
  line-height: 1;
  color: #000000;
  opacity: 0.15;
  pointer-events: none;
  user-select: none;
}

.editorial-quote__mark--open {
  top: -2rem;
  left: 0;
}

.editorial-quote__mark--close {
  bottom: -4rem;
  right: 0;
}

/* 归属信息 - 小型大写字母 */
.editorial-quote__attribution {
  margin-top: 2rem;
  font-family: 'Source Serif 4', Georgia, serif;
  font-size: 0.75rem;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.2em;
  color: #000000;
}

.editorial-quote__author {
  font-weight: 600;
}

.editorial-quote__separator {
  margin: 0 0.5rem;
}

.editorial-quote__source {
  font-style: normal;
}

/* 边框样式变体 */
.editorial-quote--bordered {
  padding: 3rem;
  border: 2px solid #000000;
}

.editorial-quote--bordered .editorial-quote__text {
  padding: 0;
}

.editorial-quote--bordered .editorial-quote__mark--open {
  top: 0.5rem;
  left: 1rem;
}

.editorial-quote--bordered .editorial-quote__mark--close {
  bottom: -2rem;
  right: 1rem;
}

/* 缩进样式变体 */
.editorial-quote--indented {
  text-align: left;
  padding-left: 4rem;
  border-left: 4px solid #000000;
}

.editorial-quote--indented .editorial-quote__text {
  padding: 0;
  text-align: left;
}

.editorial-quote--indented .editorial-quote__mark--open {
  left: -1rem;
  top: -1.5rem;
  font-size: 6rem;
}

.editorial-quote--indented .editorial-quote__mark--close {
  display: none;
}

.editorial-quote--indented .editorial-quote__attribution {
  text-align: left;
}

/* 响应式调整 */
@media (max-width: 768px) {
  .editorial-quote--xl .editorial-quote__text {
    font-size: 2.5rem;
  }

  .editorial-quote--lg .editorial-quote__text {
    font-size: 1.75rem;
  }

  .editorial-quote__mark {
    font-size: 5rem;
  }
}
</style>
```

#### 使用示例

```vue
<!-- 基础引言 -->
<EditorialQuote
  quote="设计不是它看起来怎样或感觉怎样。设计是它如何工作。"
  author="Steve Jobs"
/>

<!-- 带来源的引言 -->
<EditorialQuote
  quote="简约是终极的复杂。"
  author="Leonardo da Vinci"
  source="艺术笔记"
  size="xl"
/>

<!-- 边框样式 -->
<EditorialQuote
  quote="少即是多。"
  author="Ludwig Mies van der Rohe"
  style="bordered"
/>

<!-- 缩进样式 -->
<EditorialQuote
  quote="好的设计是尽可能少的设计。更少，但更好——因为它专注于本质方面，产品没有非本质的负担。回归纯粹，回归简单。"
  author="Dieter Rams"
  source="十项好设计原则"
  style="indented"
  size="md"
/>
```

### 7.3 DropCap 首字下沉组件

#### 设计哲学
首字下沉是古典排版艺术的标志性元素，在数字时代几乎被遗忘。Light 主题通过黑色边框强调这一传统元素，将其转化为现代极简美学的一部分——既是装饰，也是对印刷历史的致敬。

#### 组件功能
- 大号首字母（衬线字体）
- 可选边框装饰（Light 主题特色）
- 自动提取首字母
- 可配置下沉行数
- 支持手动指定首字母

#### 类型定义

```typescript
// components/theme/light/DropCap/DropCap.types.ts

export interface DropCapProps {
  letter?: string        // 手动指定首字母（可选）
  lines?: 2 | 3 | 4      // 下沉行数
  bordered?: boolean     // 是否带边框
  borderWidth?: 1 | 2 | 4 // 边框粗细
}
```

#### 组件实现

```vue
<!-- components/theme/light/DropCap/DropCap.vue -->
<script setup lang="ts">
import { computed, useSlots } from 'vue'
import type { DropCapProps } from './DropCap.types'

const props = withDefaults(defineProps<DropCapProps>(), {
  lines: 3,
  bordered: true,
  borderWidth: 2
})

const slots = useSlots()

// 从插槽内容提取首字母
const extractFirstLetter = () => {
  if (props.letter) return props.letter

  // 尝试从默认插槽提取文本
  const slotContent = slots.default?.()
  if (slotContent && slotContent[0]) {
    const text = slotContent[0].children as string
    if (typeof text === 'string') {
      return text.charAt(0).toUpperCase()
    }
  }
  return ''
}

const firstLetter = computed(() => extractFirstLetter())

// 计算首字母尺寸
const letterSizeMap = {
  2: { fontSize: '3.5rem', lineHeight: '3rem', size: '3.5rem' },
  3: { fontSize: '5rem', lineHeight: '4.5rem', size: '5rem' },
  4: { fontSize: '6.5rem', lineHeight: '6rem', size: '6.5rem' }
}

const letterStyle = computed(() => {
  const size = letterSizeMap[props.lines]
  return {
    fontSize: size.fontSize,
    lineHeight: size.lineHeight,
    width: size.size,
    height: size.size,
    borderWidth: props.bordered ? `${props.borderWidth}px` : '0'
  }
})

const dropCapClass = computed(() => [
  'drop-cap-wrapper',
  `drop-cap-wrapper--lines-${props.lines}`,
  { 'drop-cap-wrapper--bordered': props.bordered }
])
</script>

<template>
  <p :class="dropCapClass">
    <!-- 首字母 -->
    <span
      class="drop-cap"
      :style="letterStyle"
      aria-hidden="true"
    >
      {{ firstLetter }}
    </span>

    <!-- 正文内容 -->
    <span class="drop-cap-content">
      <slot />
    </span>
  </p>
</template>

<style scoped>
.drop-cap-wrapper {
  position: relative;
  margin: 0;
  font-family: 'Source Serif 4', Georgia, serif;
  font-size: 1.125rem;
  line-height: 1.75;
  color: #000000;
}

/* 首字母样式 */
.drop-cap {
  float: left;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 1rem;
  margin-bottom: 0.25rem;
  font-family: 'Playfair Display', Georgia, serif;
  font-weight: 400;
  color: #000000;
  background: transparent;
  border: 0 solid #000000;
}

/* 带边框时的样式 */
.drop-cap-wrapper--bordered .drop-cap {
  background: #FFFFFF;
}

/* 行数变体 - 调整浮动高度 */
.drop-cap-wrapper--lines-2 .drop-cap {
  margin-top: 0.125rem;
}

.drop-cap-wrapper--lines-3 .drop-cap {
  margin-top: 0.25rem;
}

.drop-cap-wrapper--lines-4 .drop-cap {
  margin-top: 0.375rem;
}

/* 正文内容 */
.drop-cap-content {
  display: block;
}

/* 首字母后的第一个字符需要移除（已在首字母中显示） */
.drop-cap-content::first-letter {
  /* 隐藏原始首字母 - 通过 letter prop 手动控制时不需要 */
}

/* 清除浮动 */
.drop-cap-wrapper::after {
  content: '';
  display: table;
  clear: both;
}

/* 悬停效果 - 边框加粗 + 反色 */
.drop-cap-wrapper--bordered .drop-cap {
  transition: none;
}

.drop-cap-wrapper--bordered:hover .drop-cap {
  background: #000000;
  color: #FFFFFF;
  border-width: 4px !important;
}

/* 焦点状态 */
.drop-cap-wrapper:focus-visible .drop-cap {
  outline: 3px solid #000000;
  outline-offset: 3px;
}
</style>
```

#### 使用示例

```vue
<!-- 基础首字下沉（3行，带边框） -->
<DropCap>
  在极简主义设计中，每一个元素都必须证明其存在的价值。没有装饰可以依赖，没有阴影可以软化边缘，每个设计决策都必须独立站稳。这是设计作为纪律——克制成为表达的最高形式。
</DropCap>

<!-- 2行下沉 -->
<DropCap :lines="2">
  简约是终极的复杂。当我们剥离所有不必要的元素，剩下的就是纯粹的本质。
</DropCap>

<!-- 4行下沉，粗边框 -->
<DropCap :lines="4" :border-width="4">
  设计的目标不是创造美丽的事物，而是创造有意义的事物。美丽只是良好设计的副产品。
</DropCap>

<!-- 无边框样式 -->
<DropCap :bordered="false">
  好的设计是尽可能少的设计。更少，但更好——因为它专注于本质方面。
</DropCap>

<!-- 手动指定首字母 -->
<DropCap letter="Q">
  uestion everything. 质疑一切是设计师最重要的特质之一。
</DropCap>
```

### 7.4 MonochromeImage 单色图像组件

#### 设计哲学
在黑白世界中，彩色图像会打破视觉一致性。默认灰度处理让图像融入整体设计语言，而悬停时恢复彩色则创造惊喜时刻——这是克制与表达的完美平衡。

#### 组件功能
- 默认灰度显示（filter: grayscale(100%)）
- 悬停恢复彩色
- 悬停时边框加粗（1px → 4px）
- 可选缩放效果
- 可选标题/说明文字
- 支持宽高比控制

#### 类型定义

```typescript
// components/theme/light/MonochromeImage/MonochromeImage.types.ts

export type AspectRatio = '1:1' | '4:3' | '16:9' | '3:2' | '2:3' | 'auto'

export interface MonochromeImageProps {
  src: string
  alt: string
  caption?: string
  credit?: string
  aspectRatio?: AspectRatio
  zoomOnHover?: boolean
  borderWidth?: 1 | 2 | 4
}
```

#### 组件实现

```vue
<!-- components/theme/light/MonochromeImage/MonochromeImage.vue -->
<script setup lang="ts">
import { computed } from 'vue'
import type { MonochromeImageProps, AspectRatio } from './MonochromeImage.types'

const props = withDefaults(defineProps<MonochromeImageProps>(), {
  aspectRatio: 'auto',
  zoomOnHover: true,
  borderWidth: 1
})

const aspectRatioMap: Record<AspectRatio, string> = {
  '1:1': '1 / 1',
  '4:3': '4 / 3',
  '16:9': '16 / 9',
  '3:2': '3 / 2',
  '2:3': '2 / 3',
  'auto': 'auto'
}

const containerStyle = computed(() => ({
  aspectRatio: aspectRatioMap[props.aspectRatio],
  '--initial-border': `${props.borderWidth}px`
}))

const imageClass = computed(() => [
  'monochrome-image',
  { 'monochrome-image--zoom': props.zoomOnHover }
])
</script>

<template>
  <figure class="monochrome-figure">
    <!-- 图片容器 -->
    <div
      :class="imageClass"
      :style="containerStyle"
    >
      <img
        :src="src"
        :alt="alt"
        class="monochrome-image__img"
        loading="lazy"
      />
    </div>

    <!-- 图片说明 -->
    <figcaption v-if="caption || credit" class="monochrome-caption">
      <span v-if="caption" class="monochrome-caption__text">{{ caption }}</span>
      <span v-if="credit" class="monochrome-caption__credit">{{ credit }}</span>
    </figcaption>
  </figure>
</template>

<style scoped>
.monochrome-figure {
  margin: 2rem 0;
  padding: 0;
}

.monochrome-image {
  position: relative;
  overflow: hidden;
  border: var(--initial-border) solid #000000;
  background: #F5F5F5;
}

/* 图片默认灰度 */
.monochrome-image__img {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
  filter: grayscale(100%);
  transition: none;  /* Light 主题即时响应 */
}

/* 悬停恢复彩色 + 边框加粗 */
.monochrome-image:hover {
  border-width: 4px;
}

.monochrome-image:hover .monochrome-image__img {
  filter: grayscale(0%);
}

/* 缩放效果 */
.monochrome-image--zoom:hover .monochrome-image__img {
  transform: scale(1.02);
}

/* 焦点状态 */
.monochrome-image:focus-within {
  outline: 3px solid #000000;
  outline-offset: 3px;
}

/* 图片说明 */
.monochrome-caption {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  margin-top: 0.75rem;
  font-family: 'Source Serif 4', Georgia, serif;
  font-size: 0.75rem;
  color: #525252;
}

.monochrome-caption__text {
  font-style: italic;
}

.monochrome-caption__credit {
  font-size: 0.625rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: #000000;
}

/* 全宽变体 */
.monochrome-figure--full {
  margin-left: -2rem;
  margin-right: -2rem;
}

.monochrome-figure--full .monochrome-caption {
  padding: 0 2rem;
}

/* 响应式 */
@media (max-width: 768px) {
  .monochrome-figure--full {
    margin-left: -1rem;
    margin-right: -1rem;
  }

  .monochrome-figure--full .monochrome-caption {
    padding: 0 1rem;
  }
}
</style>
```

#### 使用示例

```vue
<!-- 基础用法 -->
<MonochromeImage
  src="/images/course-cover.jpg"
  alt="课程封面图片"
/>

<!-- 带说明文字 -->
<MonochromeImage
  src="/images/instructor.jpg"
  alt="讲师照片"
  caption="张教授在 2024 年技术峰会上的演讲"
  credit="Photo: Wang Wei"
/>

<!-- 固定宽高比 -->
<MonochromeImage
  src="/images/classroom.jpg"
  alt="教室环境"
  aspect-ratio="16:9"
  caption="现代化智能教室"
/>

<!-- 正方形，无缩放 -->
<MonochromeImage
  src="/images/logo.jpg"
  alt="公司标志"
  aspect-ratio="1:1"
  :zoom-on-hover="false"
/>

<!-- 粗边框 -->
<MonochromeImage
  src="/images/team.jpg"
  alt="团队合影"
  :border-width="4"
  caption="核心研发团队"
/>
```

---

## 修订记录

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| v1.0.0 | 2025-12-25 | Frontend Architect | 初始版本 |
