# IT 技能培训智能选课系统 - 主题系统设计

> 文档版本：v1.0.0
> 最后更新：2025-12-25
> 文档状态：🚧 进行中
> 关联文档：[01-重构总览.md](01-重构总览.md) | [prompt-dark.md](../../../prompt-dark.md) | [prompt-light.md](../../../prompt-light.md) | [prompt-Enterprise.md](../../../prompt-Enterprise.md)

---

## 目录

1. [主题架构设计](#1-主题架构设计)
2. [Dark 主题详细设计](#2-dark-主题详细设计)
3. [Light 主题详细设计](#3-light-主题详细设计)
4. [Enterprise 主题详细设计](#4-enterprise-主题详细设计)
5. [主题切换实现](#5-主题切换实现)
6. [Tailwind 配置](#6-tailwind-配置)

---

## 1. 主题架构设计

### 1.1 主题切换机制

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           主题切换架构                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐                  │
│   │ ThemeSwitch │────>│ ThemeStore  │────>│ <html>      │                  │
│   │   组件       │     │  (Pinia)    │     │ data-theme  │                  │
│   └─────────────┘     └──────┬──────┘     └──────┬──────┘                  │
│                              │                   │                          │
│                              ▼                   ▼                          │
│                       ┌─────────────┐     ┌─────────────┐                  │
│                       │ localStorage│     │ CSS 变量    │                  │
│                       │  持久化      │     │ 自动生效    │                  │
│                       └─────────────┘     └─────────────┘                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 主题 Store 设计

```typescript
// stores/theme.ts
import { defineStore } from 'pinia'
import { ref, watch } from 'vue'

export type ThemeName = 'dark' | 'light' | 'enterprise'

export const useThemeStore = defineStore('theme', () => {
  // 从 localStorage 读取或使用默认值
  const currentTheme = ref<ThemeName>(
    (localStorage.getItem('theme') as ThemeName) || 'dark'
  )

  // 切换主题
  const setTheme = (theme: ThemeName) => {
    currentTheme.value = theme
    document.documentElement.setAttribute('data-theme', theme)
    localStorage.setItem('theme', theme)
  }

  // 初始化时应用主题
  const initTheme = () => {
    document.documentElement.setAttribute('data-theme', currentTheme.value)
  }

  // 主题配置信息
  const themeConfig = {
    dark: {
      name: 'Dark',
      label: '深色模式',
      icon: 'Moon',
      description: 'Linear/Modern 风格'
    },
    light: {
      name: 'Light',
      label: '浅色模式',
      icon: 'Sun',
      description: 'Minimalist Monochrome 风格'
    },
    enterprise: {
      name: 'Enterprise',
      label: '企业模式',
      icon: 'Building2',
      description: 'Corporate Trust 风格'
    }
  }

  return {
    currentTheme,
    setTheme,
    initTheme,
    themeConfig
  }
})
```

### 1.3 CSS 变量层级设计

```css
/* styles/themes/_variables.css */

:root {
  /* ==================== 通用变量（所有主题共享） ==================== */

  /* 字体家族 */
  --font-sans: system-ui, -apple-system, sans-serif;
  --font-mono: 'JetBrains Mono', monospace;

  /* 间距比例 */
  --spacing-xs: 0.25rem;
  --spacing-sm: 0.5rem;
  --spacing-md: 1rem;
  --spacing-lg: 1.5rem;
  --spacing-xl: 2rem;
  --spacing-2xl: 3rem;
  --spacing-3xl: 4rem;

  /* 过渡时间 */
  --transition-fast: 100ms;
  --transition-normal: 200ms;
  --transition-slow: 300ms;

  /* 层级 z-index */
  --z-dropdown: 1000;
  --z-sticky: 1100;
  --z-modal: 1200;
  --z-tooltip: 1300;
  --z-toast: 1400;
}
```

---

## 2. Dark 主题详细设计

### 2.1 设计哲学
**精准、深度、流动**——每个表面都存在于三维空间中，被柔和的环境光源照亮。设计传达"高端开发工具"——快速、响应式、精心打造。

### 2.2 颜色系统

```css
/* styles/themes/dark.css */

[data-theme="dark"] {
  /* ==================== 背景层级 ==================== */
  --background-deep: #020203;      /* 最深层 - 页脚、最底层 */
  --background-base: #050506;      /* 主画布背景 */
  --background-elevated: #0a0a0c;  /* 抬升表面、模拟界面 */
  --surface: rgba(255,255,255,0.05);        /* 卡片背景 */
  --surface-hover: rgba(255,255,255,0.08);  /* 卡片悬停 */

  /* ==================== 文字层级 ==================== */
  --foreground: #EDEDEF;                    /* 主文字 - 不是纯白 */
  --foreground-muted: #8A8F98;              /* 次要文字、描述 */
  --foreground-subtle: rgba(255,255,255,0.60); /* 三级文字、占位符 */

  /* ==================== 强调色系统 ==================== */
  --accent: #5E6AD2;                        /* 主交互色 */
  --accent-bright: #6872D9;                 /* 悬停状态 */
  --accent-glow: rgba(94,106,210,0.3);      /* 辉光效果 */
  --accent-glow-strong: rgba(94,106,210,0.5); /* 强辉光 */

  /* ==================== 边框系统 ==================== */
  --border-default: rgba(255,255,255,0.06); /* 默认边框 */
  --border-hover: rgba(255,255,255,0.10);   /* 悬停边框 */
  --border-accent: rgba(94,106,210,0.30);   /* 强调边框 */

  /* ==================== 圆角系统 ==================== */
  --radius-sm: 8px;   /* 按钮、输入框 */
  --radius-md: 12px;  /* 图标容器 */
  --radius-lg: 16px;  /* 卡片、大容器 */
  --radius-full: 9999px; /* 徽章、药丸 */

  /* ==================== 阴影系统（多层叠加） ==================== */
  /* 卡片默认阴影：边框高光 + 柔和扩散 + 环境暗影 */
  --shadow-card:
    0 0 0 1px rgba(255,255,255,0.06),
    0 2px 20px rgba(0,0,0,0.4),
    0 0 40px rgba(0,0,0,0.2);

  /* 卡片悬停阴影：增强所有层 + 添加强调色辉光 */
  --shadow-card-hover:
    0 0 0 1px rgba(255,255,255,0.1),
    0 8px 40px rgba(0,0,0,0.5),
    0 0 80px rgba(94,106,210,0.1);

  /* 按钮阴影：强调色辉光 + 内部高光 */
  --shadow-button:
    0 0 0 1px rgba(94,106,210,0.5),
    0 4px 12px rgba(94,106,210,0.3),
    inset 0 1px 0 0 rgba(255,255,255,0.2);

  /* 输入框聚焦阴影 */
  --shadow-input-focus:
    0 0 0 2px rgba(94,106,210,0.5),
    0 0 20px rgba(94,106,210,0.2);

  /* ==================== 字体系统 ==================== */
  --font-display: 'Inter', 'Geist Sans', system-ui, sans-serif;
  --font-body: 'Inter', system-ui, sans-serif;

  /* ==================== 动画时间 ==================== */
  --duration-fast: 200ms;
  --duration-normal: 300ms;
  --duration-slow: 600ms;
  --duration-blob: 8000ms;

  /* ==================== 动画缓动 ==================== */
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1); /* expo-out */
}
```

### 2.3 背景系统实现

```vue
<!-- components/theme/dark/GradientBackground.vue -->
<script setup lang="ts">
/**
 * Dark 主题多层渐变背景
 * 四层叠加：基础渐变 + 噪点纹理 + 动态光斑 + 网格覆盖
 */
</script>

<template>
  <div class="dark-background">
    <!-- Layer 1: 基础径向渐变 -->
    <div class="layer-base" />

    <!-- Layer 2: 噪点纹理 -->
    <div class="layer-noise" />

    <!-- Layer 3: 动态光斑 -->
    <div class="layer-blobs">
      <div class="blob blob-primary" />
      <div class="blob blob-secondary" />
      <div class="blob blob-tertiary" />
      <div class="blob blob-bottom" />
    </div>

    <!-- Layer 4: 网格覆盖 -->
    <div class="layer-grid" />

    <!-- 内容插槽 -->
    <div class="content-wrapper">
      <slot />
    </div>
  </div>
</template>

<style scoped>
.dark-background {
  position: relative;
  min-height: 100vh;
  background: var(--background-base);
}

/* Layer 1: 基础径向渐变 - 从顶部中心向外扩散 */
.layer-base {
  position: fixed;
  inset: 0;
  background: radial-gradient(
    ellipse at top,
    #0a0a0f 0%,
    #050506 50%,
    #020203 100%
  );
  z-index: 0;
}

/* Layer 2: 噪点纹理 - 增加质感防止色带 */
.layer-noise {
  position: fixed;
  inset: 0;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
  opacity: 0.015;
  z-index: 1;
  pointer-events: none;
}

/* Layer 3: 动态光斑容器 */
.layer-blobs {
  position: fixed;
  inset: 0;
  overflow: hidden;
  z-index: 2;
  pointer-events: none;
}

/* 主光斑 - 顶部中心，最大 */
.blob-primary {
  position: absolute;
  top: -200px;
  left: 50%;
  transform: translateX(-50%);
  width: 900px;
  height: 1400px;
  background: var(--accent);
  opacity: 0.25;
  filter: blur(150px);
  border-radius: 50%;
  animation: float 10s ease-in-out infinite;
}

/* 次光斑 - 左侧 */
.blob-secondary {
  position: absolute;
  top: 20%;
  left: -100px;
  width: 600px;
  height: 800px;
  background: linear-gradient(135deg, #7C3AED, #EC4899);
  opacity: 0.15;
  filter: blur(120px);
  border-radius: 50%;
  animation: float 12s ease-in-out infinite reverse;
}

/* 三级光斑 - 右侧 */
.blob-tertiary {
  position: absolute;
  top: 30%;
  right: -100px;
  width: 500px;
  height: 700px;
  background: linear-gradient(135deg, #3B82F6, #5E6AD2);
  opacity: 0.12;
  filter: blur(100px);
  border-radius: 50%;
  animation: float 8s ease-in-out infinite;
}

/* 底部光斑 - 脉冲动画 */
.blob-bottom {
  position: absolute;
  bottom: -200px;
  left: 30%;
  width: 600px;
  height: 400px;
  background: var(--accent);
  opacity: 0.1;
  filter: blur(100px);
  border-radius: 50%;
  animation: pulse 4s ease-in-out infinite;
}

/* Layer 4: 网格覆盖 - 技术感 */
.layer-grid {
  position: fixed;
  inset: 0;
  background-image:
    linear-gradient(rgba(255,255,255,0.02) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px);
  background-size: 64px 64px;
  z-index: 3;
  pointer-events: none;
}

/* 内容层 */
.content-wrapper {
  position: relative;
  z-index: 10;
}

/* 动画关键帧 */
@keyframes float {
  0%, 100% {
    transform: translateX(-50%) translateY(0) rotate(0deg);
  }
  50% {
    transform: translateX(-50%) translateY(-20px) rotate(1deg);
  }
}

@keyframes pulse {
  0%, 100% {
    opacity: 0.1;
    transform: scale(1);
  }
  50% {
    opacity: 0.15;
    transform: scale(1.05);
  }
}
</style>
```

### 2.4 鼠标追踪高光效果

```vue
<!-- components/theme/dark/SpotlightEffect.vue -->
<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'

const props = defineProps<{
  size?: number
  opacity?: number
}>()

const spotlightRef = ref<HTMLElement | null>(null)
const mouseX = ref(0)
const mouseY = ref(0)
const isHovering = ref(false)

const handleMouseMove = (e: MouseEvent) => {
  if (!spotlightRef.value) return
  const rect = spotlightRef.value.getBoundingClientRect()
  mouseX.value = e.clientX - rect.left
  mouseY.value = e.clientY - rect.top
}

const handleMouseEnter = () => {
  isHovering.value = true
}

const handleMouseLeave = () => {
  isHovering.value = false
}
</script>

<template>
  <div
    ref="spotlightRef"
    class="spotlight-container"
    @mousemove="handleMouseMove"
    @mouseenter="handleMouseEnter"
    @mouseleave="handleMouseLeave"
  >
    <!-- 高光效果层 -->
    <div
      class="spotlight"
      :style="{
        '--x': `${mouseX}px`,
        '--y': `${mouseY}px`,
        '--size': `${size || 300}px`,
        '--opacity': isHovering ? (opacity || 0.15) : 0
      }"
    />
    <slot />
  </div>
</template>

<style scoped>
.spotlight-container {
  position: relative;
  overflow: hidden;
}

.spotlight {
  position: absolute;
  inset: 0;
  pointer-events: none;
  background: radial-gradient(
    var(--size) circle at var(--x) var(--y),
    var(--accent-glow),
    transparent 50%
  );
  opacity: var(--opacity);
  transition: opacity 200ms ease-out;
  z-index: 1;
}
</style>
```

---

## 3. Light 主题详细设计

### 3.1 设计哲学
**还原本质**——极简单色设计剥离一切装饰，没有强调色可以依赖，没有渐变可以软化边缘，没有阴影可以创造假深度。每个设计决策都必须独立站稳。这是设计作为纪律——克制成为表达的最高形式。

### 3.2 颜色系统

```css
/* styles/themes/light.css */

[data-theme="light"] {
  /* ==================== 纯黑白色板 ==================== */
  --background: #FFFFFF;              /* 纯白背景 */
  --foreground: #000000;              /* 纯黑文字 */
  --muted: #F5F5F5;                   /* 微灰背景 */
  --muted-foreground: #525252;        /* 暗灰二级文字 */

  /* ==================== 强调色 = 黑色 ==================== */
  --accent: #000000;                  /* 黑色即强调色 */
  --accent-foreground: #FFFFFF;       /* 黑底白字 */

  /* ==================== 边框系统（线条分级） ==================== */
  --border-hairline: #E5E5E5;         /* 发丝线 - 微妙分隔 */
  --border-thin: #000000;             /* 细线 - 标准边框 */
  --border-width-hairline: 1px;
  --border-width-thin: 1px;
  --border-width-medium: 2px;
  --border-width-thick: 4px;
  --border-width-ultra: 8px;

  /* ==================== 圆角系统（全部为 0） ==================== */
  --radius-sm: 0;
  --radius-md: 0;
  --radius-lg: 0;
  --radius-full: 0;  /* 即使是"圆"也是方的 */

  /* ==================== 阴影系统（无阴影） ==================== */
  --shadow-card: none;
  --shadow-card-hover: none;
  --shadow-button: none;
  --shadow-input-focus: none;

  /* ==================== 字体系统（衬线为主） ==================== */
  --font-display: 'Playfair Display', Georgia, serif;
  --font-body: 'Source Serif 4', Georgia, serif;
  --font-mono: 'JetBrains Mono', monospace;

  /* ==================== 动画时间（极短/即时） ==================== */
  --duration-fast: 0ms;     /* 即时 */
  --duration-normal: 100ms; /* 最多 100ms */
  --duration-slow: 100ms;   /* 不超过 100ms */

  /* ==================== 动画缓动（无缓动） ==================== */
  --ease-out: linear;  /* 或直接 step-end */

  /* ==================== 焦点状态（无障碍必须） ==================== */
  --focus-ring: 3px solid #000000;  /* 粗实线焦点环 */
  --focus-offset: 3px;              /* 焦点环偏移 */
}
```

### 3.3 线条系统组件

```vue
<!-- components/theme/light/LineRule.vue -->
<script setup lang="ts">
/**
 * Light 主题线条分隔组件
 * 线条是 Light 主题的核心视觉元素
 */
const props = defineProps<{
  weight?: 'hairline' | 'thin' | 'medium' | 'thick' | 'ultra'
  direction?: 'horizontal' | 'vertical'
}>()

const weightMap = {
  hairline: 'h-px bg-[#E5E5E5]',
  thin: 'h-px bg-black',
  medium: 'h-0.5 bg-black',
  thick: 'h-1 bg-black',
  ultra: 'h-2 bg-black'
}

const verticalWeightMap = {
  hairline: 'w-px bg-[#E5E5E5]',
  thin: 'w-px bg-black',
  medium: 'w-0.5 bg-black',
  thick: 'w-1 bg-black',
  ultra: 'w-2 bg-black'
}
</script>

<template>
  <div
    :class="[
      direction === 'vertical'
        ? verticalWeightMap[weight || 'thin']
        : weightMap[weight || 'thin'],
      direction === 'vertical' ? 'h-full' : 'w-full'
    ]"
    role="separator"
  />
</template>
```

### 3.4 反转区块组件

```vue
<!-- components/theme/light/InvertedSection.vue -->
<script setup lang="ts">
/**
 * Light 主题反转区块
 * 用于强调重要内容 - 黑底白字
 */
defineProps<{
  withTexture?: boolean
}>()
</script>

<template>
  <section class="inverted-section">
    <!-- 纹理层（可选） -->
    <div v-if="withTexture" class="texture-overlay" />

    <!-- 内容 -->
    <div class="content">
      <slot />
    </div>
  </section>
</template>

<style scoped>
.inverted-section {
  position: relative;
  background: #000000;
  color: #FFFFFF;
  padding: 6rem 0;
}

/* 反转区块内的所有文字强制白色 */
.inverted-section :deep(*) {
  color: #FFFFFF;
}

/* 垂直线条纹理 */
.texture-overlay {
  position: absolute;
  inset: 0;
  background-image: repeating-linear-gradient(
    90deg,
    transparent,
    transparent 1px,
    #fff 1px,
    #fff 2px
  );
  background-size: 4px 100%;
  opacity: 0.03;
  pointer-events: none;
}

.content {
  position: relative;
  z-index: 1;
}
</style>
```

### 3.5 纹理覆盖组件

```vue
<!-- components/theme/light/TextureOverlay.vue -->
<script setup lang="ts">
/**
 * Light 主题纹理覆盖
 * 防止设计过于平面化
 */
defineProps<{
  type?: 'horizontal' | 'grid' | 'diagonal' | 'noise'
  opacity?: number
}>()
</script>

<template>
  <div
    class="texture-overlay"
    :class="`texture-${type || 'horizontal'}`"
    :style="{ '--opacity': opacity || 0.015 }"
  />
</template>

<style scoped>
.texture-overlay {
  position: absolute;
  inset: 0;
  pointer-events: none;
  opacity: var(--opacity);
}

/* 水平线条 */
.texture-horizontal {
  background-image: repeating-linear-gradient(
    0deg,
    transparent,
    transparent 1px,
    #000 1px,
    #000 2px
  );
  background-size: 100% 4px;
}

/* 网格 */
.texture-grid {
  background-image:
    linear-gradient(#00000008 1px, transparent 1px),
    linear-gradient(90deg, #00000008 1px, transparent 1px);
  background-size: 40px 40px;
}

/* 对角线 */
.texture-diagonal {
  background-image: repeating-linear-gradient(
    45deg,
    transparent,
    transparent 40px,
    #00000008 40px,
    #00000008 42px
  );
}

/* 噪点 */
.texture-noise {
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
  opacity: 0.02;
}
</style>
```

### 3.6 Typography 系统

```css
/* Light 主题字体尺度（戏剧性范围） */

[data-theme="light"] {
  /* 超大标题 - 用于 Hero 区域 */
  --text-9xl: 10rem;   /* 160px - 超大声明 */
  --text-8xl: 8rem;    /* 128px - 展示标题 */
  --text-7xl: 6rem;    /* 96px - Hero 标题 */
  --text-6xl: 4.5rem;  /* 72px - Hero 副标题 */
  --text-5xl: 3.5rem;  /* 56px - 页面标题 */
  --text-4xl: 2.5rem;  /* 40px - 区域标题 */
  --text-3xl: 2rem;    /* 32px - 小标题 */
  --text-2xl: 1.5rem;  /* 24px - 区域引言 */
  --text-xl: 1.25rem;  /* 20px - 引导段落 */
  --text-lg: 1.125rem; /* 18px - 正文首选 */
  --text-base: 1rem;   /* 16px - 正文最小 */
  --text-sm: 0.875rem; /* 14px - 标签、说明 */
  --text-xs: 0.75rem;  /* 12px - 元数据 */
}

/* 字间距 */
.tracking-tighter { letter-spacing: -0.05em; }  /* 大标题 */
.tracking-tight { letter-spacing: -0.025em; }   /* 标题 */
.tracking-normal { letter-spacing: 0; }          /* 正文 */
.tracking-widest { letter-spacing: 0.1em; }      /* 小型大写 */

/* 行高 */
.leading-none { line-height: 1; }        /* 展示标题 */
.leading-tight { line-height: 1.25; }    /* 紧凑标题 */
.leading-relaxed { line-height: 1.625; } /* 正文 */
```

---

## 4. Enterprise 主题详细设计

### 4.1 设计哲学
**现代企业 SaaS 美学**——专业但平易近人，精致但友好。这种设计拒绝传统企业网站冷淡、无菌的正式感，转而采用温暖、自信、令人愉悦的视觉语言。

### 4.2 颜色系统

```css
/* styles/themes/enterprise.css */

[data-theme="enterprise"] {
  /* ==================== 背景系统 ==================== */
  --background: #F8FAFC;              /* Slate 50 - 微冷灰白基底 */
  --foreground: #FFFFFF;              /* 卡片/表面 - 纯白 */

  /* ==================== 主色系统（Indigo + Violet） ==================== */
  --primary: #4F46E5;                 /* Indigo 600 - 核心品牌色 */
  --primary-hover: #4338CA;           /* Indigo 700 - 悬停状态 */
  --secondary: #7C3AED;               /* Violet 600 - 渐变辅色 */
  --gradient-primary: linear-gradient(135deg, #4F46E5, #7C3AED);

  /* ==================== 文字系统 ==================== */
  --text-main: #0F172A;               /* Slate 900 - 高对比主文字 */
  --text-muted: #64748B;              /* Slate 500 - 次要文字 */
  --text-on-primary: #FFFFFF;         /* 主色上的文字 */

  /* ==================== 功能色 ==================== */
  --success: #10B981;                 /* Emerald 500 */
  --warning: #F59E0B;                 /* Amber 500 */
  --error: #EF4444;                   /* Red 500 */
  --info: #3B82F6;                    /* Blue 500 */

  /* ==================== 边框系统 ==================== */
  --border: #E2E8F0;                  /* Slate 200 */
  --border-hover: #CBD5E1;            /* Slate 300 */

  /* ==================== 圆角系统 ==================== */
  --radius-sm: 8px;                   /* 输入框 */
  --radius-md: 12px;                  /* 卡片 */
  --radius-lg: 9999px;                /* 全圆按钮 */

  /* ==================== 彩色阴影系统（核心特色！） ==================== */
  /* 卡片默认 - 蓝紫色调阴影 */
  --shadow-card: 0 4px 20px -2px rgba(79, 70, 229, 0.1);

  /* 卡片悬停 - 多层增强 */
  --shadow-card-hover:
    0 10px 25px -5px rgba(79, 70, 229, 0.15),
    0 8px 10px -6px rgba(79, 70, 229, 0.1);

  /* 按钮阴影 - 强存在感 */
  --shadow-button: 0 4px 14px 0 rgba(79, 70, 229, 0.3);

  /* 辉光效果 - 徽章/数字 */
  --shadow-glow: 0 0 20px rgba(79, 70, 229, 0.5);

  /* ==================== 字体系统 ==================== */
  --font-display: 'Plus Jakarta Sans', system-ui, sans-serif;
  --font-body: 'Plus Jakarta Sans', system-ui, sans-serif;

  /* ==================== 动画时间 ==================== */
  --duration-fast: 200ms;
  --duration-normal: 300ms;
  --duration-slow: 500ms;

  /* ==================== 动画缓动 ==================== */
  --ease-out: ease-out;

  /* ==================== 等距变换（3D 卡片效果） ==================== */
  --isometric-rotate-x: 5deg;
  --isometric-rotate-y: -12deg;
  --isometric-rotate-x-hover: 2deg;
  --isometric-rotate-y-hover: -8deg;
  --perspective: 2000px;
}
```

### 4.3 渐变光晕背景

```vue
<!-- components/theme/enterprise/GradientOrbs.vue -->
<script setup lang="ts">
/**
 * Enterprise 主题渐变光晕背景
 * 大型模糊渐变球创造氛围深度
 */
</script>

<template>
  <div class="gradient-orbs">
    <!-- 主光晕 - 右上角 -->
    <div class="orb orb-primary" />

    <!-- 次光晕 - 左下角 -->
    <div class="orb orb-secondary" />

    <!-- 脉冲光晕 - 中心偏右 -->
    <div class="orb orb-pulse" />
  </div>
</template>

<style scoped>
.gradient-orbs {
  position: absolute;
  inset: 0;
  overflow: hidden;
  pointer-events: none;
  z-index: 0;
}

.orb {
  position: absolute;
  border-radius: 50%;
}

/* 主光晕 - Indigo */
.orb-primary {
  top: -200px;
  right: -100px;
  width: 600px;
  height: 600px;
  background: linear-gradient(135deg, #4F46E5 0%, #7C3AED 100%);
  opacity: 0.2;
  filter: blur(100px);
}

/* 次光晕 - Violet */
.orb-secondary {
  bottom: -150px;
  left: -100px;
  width: 400px;
  height: 400px;
  background: linear-gradient(135deg, #7C3AED 0%, #EC4899 100%);
  opacity: 0.15;
  filter: blur(80px);
}

/* 脉冲光晕 */
.orb-pulse {
  top: 40%;
  right: 20%;
  width: 300px;
  height: 300px;
  background: #4F46E5;
  opacity: 0.1;
  filter: blur(60px);
  animation: pulse 4s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 0.1; transform: scale(1); }
  50% { opacity: 0.15; transform: scale(1.1); }
}
</style>
```

### 4.4 等距卡片组件

```vue
<!-- components/theme/enterprise/IsometricCard.vue -->
<script setup lang="ts">
/**
 * Enterprise 主题等距卡片
 * 3D 变换创造视觉深度
 */
defineProps<{
  rotateY?: number
  rotateX?: number
  floating?: boolean
}>()
</script>

<template>
  <div
    class="isometric-wrapper"
    :class="{ 'animate-float': floating }"
  >
    <div
      class="isometric-card"
      :style="{
        '--rotate-x': `${rotateX || 5}deg`,
        '--rotate-y': `${rotateY || -12}deg`
      }"
    >
      <slot />
    </div>
  </div>
</template>

<style scoped>
.isometric-wrapper {
  perspective: 2000px;
}

.isometric-card {
  transform: rotateX(var(--rotate-x)) rotateY(var(--rotate-y));
  transition: transform 300ms ease-out;
  transform-style: preserve-3d;
}

.isometric-card:hover {
  transform: rotateX(2deg) rotateY(-8deg);
}

.animate-float {
  animation: float 4s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}
</style>
```

### 4.5 彩色阴影卡片

```vue
<!-- components/theme/enterprise/ColoredShadowCard.vue -->
<script setup lang="ts">
/**
 * Enterprise 主题彩色阴影卡片
 * 蓝紫色调阴影替代中性灰
 */
defineProps<{
  elevated?: boolean
}>()
</script>

<template>
  <div
    class="colored-shadow-card"
    :class="{ elevated }"
  >
    <slot />
  </div>
</template>

<style scoped>
.colored-shadow-card {
  background: white;
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  padding: 1.5rem;
  transition: all 200ms ease-out;

  /* 默认彩色阴影 */
  box-shadow: var(--shadow-card);
}

.colored-shadow-card:hover {
  /* 悬停时抬升 + 增强阴影 */
  transform: translateY(-4px);
  box-shadow: var(--shadow-card-hover);
  border-color: var(--border-hover);
}

/* 突出显示变体 */
.colored-shadow-card.elevated {
  transform: scale(1.05);
  box-shadow:
    var(--shadow-card-hover),
    0 0 0 4px rgba(79, 70, 229, 0.1);
}
</style>
```

---

## 5. 主题切换实现

### 5.1 主题切换器组件

```vue
<!-- components/base/ThemeSwitch.vue -->
<script setup lang="ts">
import { useThemeStore, type ThemeName } from '@/stores/theme'
import { Moon, Sun, Building2 } from 'lucide-vue-next'

const themeStore = useThemeStore()

const themes: { key: ThemeName; icon: any; label: string }[] = [
  { key: 'dark', icon: Moon, label: '深色' },
  { key: 'light', icon: Sun, label: '浅色' },
  { key: 'enterprise', icon: Building2, label: '企业' }
]
</script>

<template>
  <div class="theme-switch">
    <button
      v-for="theme in themes"
      :key="theme.key"
      class="theme-btn"
      :class="{ active: themeStore.currentTheme === theme.key }"
      :title="theme.label"
      @click="themeStore.setTheme(theme.key)"
    >
      <component :is="theme.icon" :size="18" />
    </button>
  </div>
</template>

<style scoped>
.theme-switch {
  display: flex;
  gap: 0.25rem;
  padding: 0.25rem;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
}

.theme-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 36px;
  height: 36px;
  border: none;
  background: transparent;
  color: var(--foreground-muted);
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: all var(--duration-fast) var(--ease-out);
}

.theme-btn:hover {
  background: var(--surface-hover);
  color: var(--foreground);
}

.theme-btn.active {
  background: var(--accent);
  color: var(--accent-foreground);
}
</style>
```

### 5.2 主题初始化

```typescript
// main.ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import router from './router'
import { useThemeStore } from './stores/theme'

// 导入样式
import './styles/main.css'

const app = createApp(App)
const pinia = createPinia()

app.use(pinia)
app.use(router)

// 初始化主题
const themeStore = useThemeStore()
themeStore.initTheme()

app.mount('#app')
```

### 5.3 CSS 入口文件

```css
/* styles/main.css */

/* 字体定义 */
@import './fonts.css';

/* 主题变量 */
@import './themes/_variables.css';
@import './themes/dark.css';
@import './themes/light.css';
@import './themes/enterprise.css';

/* 基础样式 */
@import './base.css';

/* 动画 */
@import './animations.css';

/* Tailwind */
@tailwind base;
@tailwind components;
@tailwind utilities;
```

---

## 6. Tailwind 配置

### 6.1 tailwind.config.ts

```typescript
// tailwind.config.ts
import type { Config } from 'tailwindcss'

export default {
  content: [
    './index.html',
    './src/**/*.{vue,js,ts,jsx,tsx}'
  ],
  theme: {
    extend: {
      // 使用 CSS 变量的颜色
      colors: {
        background: 'var(--background)',
        foreground: 'var(--foreground)',
        muted: 'var(--muted)',
        'muted-foreground': 'var(--muted-foreground)',
        accent: 'var(--accent)',
        'accent-foreground': 'var(--accent-foreground)',
        border: 'var(--border)',
        primary: 'var(--primary)',
        secondary: 'var(--secondary)',
      },

      // 使用 CSS 变量的圆角
      borderRadius: {
        sm: 'var(--radius-sm)',
        md: 'var(--radius-md)',
        lg: 'var(--radius-lg)',
      },

      // 使用 CSS 变量的阴影
      boxShadow: {
        card: 'var(--shadow-card)',
        'card-hover': 'var(--shadow-card-hover)',
        button: 'var(--shadow-button)',
      },

      // 字体家族
      fontFamily: {
        display: 'var(--font-display)',
        body: 'var(--font-body)',
        mono: 'var(--font-mono)',
      },

      // 动画
      transitionDuration: {
        fast: 'var(--duration-fast)',
        normal: 'var(--duration-normal)',
        slow: 'var(--duration-slow)',
      },

      // Dark 主题超大字体
      fontSize: {
        '9xl': ['10rem', { lineHeight: '1' }],
        '10xl': ['12rem', { lineHeight: '1' }],
      },

      // 动画关键帧
      keyframes: {
        float: {
          '0%, 100%': { transform: 'translateY(0) rotate(0deg)' },
          '50%': { transform: 'translateY(-20px) rotate(1deg)' },
        },
        pulse: {
          '0%, 100%': { opacity: '0.1', transform: 'scale(1)' },
          '50%': { opacity: '0.15', transform: 'scale(1.05)' },
        },
        'shimmer': {
          '0%': { backgroundPosition: '-200% 0' },
          '100%': { backgroundPosition: '200% 0' },
        },
      },
      animation: {
        float: 'float 10s ease-in-out infinite',
        'pulse-slow': 'pulse 4s ease-in-out infinite',
        shimmer: 'shimmer 2s linear infinite',
      },
    },
  },
  plugins: [],
} satisfies Config
```

---

## 修订记录

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| v1.0.0 | 2025-12-25 | Frontend Architect | 初始版本 |

